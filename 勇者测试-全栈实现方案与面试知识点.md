# 勇者测试 · 全栈实现方案与面试知识点

> 基于仓库真实代码（`mqq0000/brave-test`）：后端 25 张表 / 8 大 Service / 40+ 接口，前端 8 页面，全流程经过 13 单测 + 38 API 回归 + 12 浏览器 E2E 验证。

---

# 第一部分 · 总体实现方案

## 1.1 技术选型与理由（面试常问"为什么选 X"）

| 层 | 选型 | 一句话理由 |
|---|---|---|
| 后端框架 | SpringBoot 3.3.4 | 约定优于配置，内嵌 Tomcat，生态最全；3.x 基于 JDK 17 虚拟线程就绪 |
| ORM | MyBatis-Plus 3.5.7 | 单表 CRUD 零 SQL（LambdaQueryWrapper），复杂查询仍可写 XML，灵活度不丢 |
| 数据库 | MySQL 8（utf8mb4） | 事务/外键/行级锁，金额对账必须 ACID |
| 缓存/锁 | Redis 7 | ZSet 做排行榜 O(logN)；SETNX 做接单互斥锁 |
| 对象存储 | MinIO | 私有桶 + 预签名 URL，正文/图片不进数据库 |
| 认证 | JWT (jjwt 0.12) | 无状态、水平扩展友好，拦截器统一校验 |
| 文档 | Knife4j 4.5 | springdoc 增强 UI，注解即文档 |
| 前端框架 | Vue 3.4 `<script setup>` | Composition API 逻辑聚合，编译产物更小 |
| UI / 状态 / 路由 | Element Plus / Pinia / Vue Router 4 | 中后台组件全；Pinia 替代 Vuex（TS 友好、无 mutation） |
| 构建 | Vite 5 | ESM 按需编译，冷启动秒级 |
| 部署 | Docker Compose 五服务 | 一键起全套，`initdb` 自动建库导种子数据 |
| CI | GitHub Actions | push 触发 mvn test + npm build 双 job 门禁 |

## 1.2 一次完整请求的拓扑

```
浏览器 (Vue3 SPA)
   │  axios (baseURL=/api/v1, 自动带 Bearer token)
   ▼
Nginx (:80)  ── 静态资源直接返回；/api 反向代理 ──┐
                                                ▼
                                    SpringBoot (:8080)
                                    JwtInterceptor 校验 token → UserContext(ThreadLocal)
                                            │
                          Controller(参数校验) → Service(业务/事务) → Mapper(MyBatis-Plus)
                                            │
                          MySQL(数据)  Redis(锁/排行)  MinIO(文件/签名URL)
```

开发环境由 Vite dev server 的 `proxy` 把 `/api` 转到 8080；生产由 Nginx 承担同样角色——**前端永远只写相对路径 `/api/v1`，环境无感知**。

---

# 第二部分 · 后端实现方案

## 2.1 分层结构

```
com.bravetest
├── common/      R(统一响应) BusinessException GlobalExceptionHandler UserContext
├── security/    JwtUtil(签发/解析)  JwtInterceptor(拦截+注入用户)
├── config/      WebMvc(拦截器注册/放行清单) MybatisPlus Redis OpenApi Minio Jackson
├── entity/      25 个 @TableName 实体（雪花 ID、逻辑删除）
├── mapper/      BaseMapper 泛型继承，无 XML
├── dto/         入参校验对象（@NotBlank + @Valid）
├── service/     Auth/Adventurer/Task/Settlement/Finance/Shop/Knowledge/Story/Test/Minio/SelfMotivation
└── controller/  Auth/Adventurer/Task/Admin/Shop/Knowledge/Story/Test/SelfMotivation
```

## 2.2 统一响应与全局异常（前后端契约的根基）

```java
public class R<T> {
    private int code;        // 0=成功，40x/500=失败
    private String message;
    private T data;
    public static <T> R<T> ok(T data) { ... }
    public static <T> R<T> error(int code, String msg) { ... }
}

@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)          // 业务异常 → code 原样返回
    @ExceptionHandler(MethodArgumentNotValidException.class) // @Valid 失败 → 400 + 首个字段提示
    @ExceptionHandler(Exception.class)                  // 兜底 → 500 "系统繁忙"（不泄露堆栈）
}
```

**要点**：前端只需要判断 `code !== 0`；错误消息统一中文可读；堆栈只进日志不进响应（安全）。

## 2.3 JWT 认证链路

```java
// 签发（登录成功）
String token = Jwts.builder()
        .subject(user.getId().toString())
        .claim("role", user.getRole())
        .issuedAt(new Date())
        .expiration(Date.from(Instant.now().plus(Duration.ofHours(72))))
        .signWith(key)                    // HS256，密钥来自配置
        .compact();

// 校验（JwtInterceptor，除登录/注册/doc.html 外全拦截）
String token = request.getHeader("Authorization");      // 必须带 "Bearer " 前缀
Claims c = Jwts.parser().verifyWith(key).build()
                .parseSignedClaims(token).getPayload();
UserContext.set(Long.parseLong(c.getSubject()), c.get("role", String.class)); // ThreadLocal
```

**要点**：
- `UserContext` 是 `ThreadLocal` 封装，Service 层不接触 HttpServletRequest；**请求结束时必须 remove，防止线程池复用导致串号**（拦截器 `afterCompletion` 清理）。
- 无状态 JWT 的代价：**登出/封禁无法立刻失效**——项目用"72h 短有效期 + 数据库 role 每次校验"折衷；面试可答"严格的场景加 Redis 黑名单"。

## 2.4 三个"含金量"并发设计（面试必讲）

### ① 接单防超抢（Redis SETNX + DB 条件更新双保险）

```java
String lockKey = "lock:task:accept:" + taskId;
Boolean locked = redis.opsForValue().setIfAbsent(lockKey, userId, 10, SECONDS);
if (!Boolean.TRUE.equals(locked)) throw new BusinessException("手慢了，任务刚被抢走");
try {
    // 锁内再校验：等级、赤化值、任务状态
    // 最后用 DB 条件更新兜底（即使锁失效也不会一单多人）
    int rows = taskMapper.update(null,
        new LambdaUpdateWrapper<Task>()
            .eq(Task::getId, taskId)
            .eq(Task::getStatus, 1)              // 仍是"待接"
            .set(Task::getStatus, 2)
            .set(Task::getAdventurerId, myAdvId));
    if (rows == 0) throw new BusinessException("任务已被接取");
} finally {
    redis.delete(lockKey);                        // 必须释放
}
```

**面试高光**：锁在事务提交**前**释放存在竞态窗口——所以 DB 层 `eq(status,1)` 条件更新才是最终防线，Redis 锁只是把"无效重试"挡在门外（削流量），这就是"双保险"的分工。

### ② 乐观扣款防超支

```java
int rows = adventurerMapper.update(null,
    new LambdaUpdateWrapper<Adventurer>()
        .eq(Adventurer::getId, advId)
        .ge(Adventurer::getGoldBalance, cost)     // 余额充足才扣
        .setSql("gold_balance = gold_balance - " + cost));
if (rows == 0) throw new BusinessException("金币不足");
```

不用 `SELECT` 后内存比较再 `UPDATE`（并发下会超支），也不引悲观锁（`SELECT FOR UPDATE` 锁范围大）。**一条 UPDATE 在行锁保护下原子完成"检查+扣减"**。

### ③ 月度结算幂等

`settlement_log` 表建 `(month, type)` 唯一键，Job 先 insert 该键：插得进去才执行结算、插冲突直接跳过。**Job 重复调度/手动重跑都不会重复发钱**。

## 2.5 资金一致性：流水表 + 余额快照

`gold_flow` 每笔记录 `balance_before / amount / balance_after`：
- 可对账：任何时刻 `sum(amount) == 最新余额 - 初始余额`
- 可审计：协会账本 `association_ledger` 按月汇总
- 金额一律 **long（分/金币整数）**，避免浮点误差——面试常问"金额为什么不用 Double"

## 2.6 MinIO 签名 URL（防盗链 + 借阅期联动）

```java
String url = minioClient.getPresignedObjectUrl(
    GetPresignedObjectUrlArgs.builder()
        .method(Method.GET).bucket(bucket).object(objectName)
        .expiry(borrowRemainSeconds, SECONDS)   // 借阅剩多久，URL 就活多久
        .build());
```

**容器经典坑**：MinIO 按 host 签名，容器内 endpoint 是 `minio:9000`，浏览器拿到的 URL 打不开 → 读 `minio.public-endpoint` 配置对 URL 做 host 重写为 `localhost:9000`。

## 2.7 数据库设计要点（25 张表）

| 设计决策 | 落地 |
|---|---|
| 主键 | 雪花 ID（`assign_id`），分库分表友好——**代价是前端 JS 精度问题，见 4.4** |
| 逻辑删除 | `deleted` 字段 + MP `@TableLogic`，删除不留物理空洞 |
| 状态机落库 | task.status(1待接/2进行/3待审/4完成/5失败)、report.status(0待处理/1已处罚/2已驳回) 全用 TINYINT + 注释 |
| 剧情三层 | chapter → story_node(选项 JSON) → player_progress(每用户每章节进度) |
| 审计 | 每表 `created_at`，关键表带 `handler_id / handle_remark` |

---

# 第三部分 · 前端实现方案

## 3.1 工程结构

```
src/
├── main.js          createApp + ElementPlus + Pinia + Router + 全局图标
├── router/index.js  路由表 + 全局守卫（登录校验 / 角色校验）
├── api/
│   ├── request.js   axios 实例 + 双拦截器（★ 前后端互动核心）
│   └── index.js     40+ 接口函数，按业务分组
├── views/           Login/Layout/TaskHall/Shop/Knowledge/Profile/Admin/Story/SelfMotivation
└── vite.config.js   dev 代理 /api → localhost:8080
```

## 3.2 axios 双拦截器（整个互动的"咽喉"）

```js
// 请求拦截：统一塞 token
request.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = 'Bearer ' + token
  return config
})

// 响应拦截：统一拆 R 包裹 + 统一错误提示
request.interceptors.response.use(
  (resp) => {
    const body = resp.data
    if (body.code !== 0) {                       // HTTP 200 但业务失败
      ElMessage.error(body.message || '请求失败')
      return Promise.reject(new Error(body.message))
    }
    return body.data                              // ★ 直接返回 data，页面少一层 .data
  },
  (error) => {
    if (error.response?.status === 401) {         // token 过期 → 清除并踢回登录
      localStorage.removeItem('token')
      router.push('/login')
      ElMessage.error('登录已过期，请重新登录')
    } else {
      ElMessage.error(error.response?.data?.message || '网络异常，请稍后再试')
    }
    return Promise.reject(error)
  }
)
```

**三层错误分工**：HTTP 层(401/500) → 拦截器兜底；业务层(code≠0) → 拦截器弹消息 + reject；页面层 → 只写 `try/catch` 成功路径。**全站没有一个页面手写错误提示**。

## 3.3 路由守卫（前端鉴权）

```js
router.beforeEach((to) => {
  if (to.name !== 'login' && !localStorage.getItem('token')) return { name: 'login' }
  if (to.name === 'admin') {
    const admins = ['SUPER_ADMIN', 'XINGZHE_ADMIN', 'ADVENTURER_ADMIN', 'KNOWLEDGE_ADMIN']
    if (!admins.includes(localStorage.getItem('role') || '')) return { name: 'tasks' }
  }
  return true
})
```

**面试要点**：前端守卫只是**体验优化**（少看到白屏/报错），真正的安全边界在**后端拦截器**——拿 curl 直打 admin 接口照样 403，因为 JwtInterceptor 解出 role 后 `requireRole()` 二次校验。"前端鉴权可被绕过，所以后端必须重复校验"是标准答案。

## 3.4 组件层模式（`<script setup>` + 组合式）

```js
const tasks = ref([])
const loadTasks = async () => { tasks.value = await selfTasks() }   // api 层已拆包，直接拿数组
const doComplete = async (t) => {
  const res = await completeSelfTask(t.id)      // POST → 后端返回本次收益明细
  ElMessage.success(`获得 ${res.reward} 修行币`) // 即时反馈
  loadTasks(); loadBalance()                     // 刷新列表 + 余额卡片（重查询保证一致）
}
onMounted(loadTasks)
```

要点：**"操作成功后重查询"而非前端手算余额**——前端算术一旦与后端事务有偏差就会越走越歪，重查询永远和账本一致。

## 3.5 生产部署形态（Nginx）

```nginx
location / { try_files $uri $uri/ /index.html; }   # SPA history 路由，刷新不 404
location /api { proxy_pass http://backend:8080; }  # 容器网络直连，同源无 CORS
```

**同源部署 = 根本上消灭 CORS**：前端和 API 同一个域（`localhost:80`），预检请求都不会出现。开发环境的"跨域"由 Vite proxy 用**服务器端转发**绕过（同源策略只约束浏览器）。

---

# 第四部分 · 前后端互动全过程（面试重点叙述题）

## 4.1 登录的完整时序（背下来，能画出来）

```
1. Login.vue 提交表单 → POST /api/v1/auth/login {username, password}
2. Vite proxy / Nginx 反代 → SpringBoot
3. AuthController → AuthService：
   ├─ 查 user 表（BCrypt.matches 校验密码，库里只存密文）
   └─ 签发 JWT（sub=userId, claim=role, 72h）
4. 返回 R.ok({token, role, nickname})
5. 前端：localStorage 存 token/role → router.push('/tasks')
6. 之后每个请求：请求拦截器自动加 Authorization: Bearer xxx
7. JwtInterceptor 每次请求：验签 → 取 userId/role → ThreadLocal → Controller 随取随用
```

## 4.2 业务写操作的时序（以"兑换自我奖励"为例）

```
前端 doRedeem(item)
  → POST /self/shop/items/{id}/redeem
  → Controller: UserContext.getUserId() 拿当前用户（不信任前端传 id！）
  → Service @Transactional:
      ① 乐观扣款（余额足够才扣，rows==0 抛 BusinessException）
      ② 写 self_redemption 兑换记录（含 10% 会费明细）
      ③ 写 gold_flow 流水（带余额快照）
      ④ association_ledger 协会入账
      —— 四步同事务，任何一步抛异常全部回滚
  → 返回 R.ok({itemName, cost, assocFee})
  → 前端 ElMessage 提示 → loadShop() + loadBalance() 重查询刷新
```

## 4.3 并发互动（接单）的前后端配合

前端"接单"按钮 → 后端返回明确失败原因（"任务已被接取"/"等级不足"）→ 拦截器统一弹 toast → 前端 `loadTasks()` 刷新列表，**UI 永远以后端返回为准**，不做本地乐观修改任务状态。

## 4.4 大整数精度坑（真实踩过，极佳面试素材）

**现象**：curl 测试一切正常，浏览器里购买却报"信息集不存在或已下架"。

**根因**：雪花 ID 19 位（如 `2098758808064270338`）超出 JS `Number.MAX_SAFE_INTEGER`（16 位），`JSON.parse` 静默把尾数舍入成 `...0300`，前端拿错误 id 回传 → 后端查无记录。

**修复**：后端加 `JacksonConfig`，**Long 统一序列化为字符串**：

```java
@Bean
public Jackson2ObjectMapperBuilderCustomizer customizer() {
    return builder -> builder
        .serializerByType(Long.class, ToStringSerializer.instance)
        .serializerByType(Long.TYPE, ToStringSerializer.instance);
}
```

前端 id 只做透传与 `===` 比较，不受影响；Spring 收到字符串自动转回 Long。**教训**：雪花 ID 项目必须前后端约定字符串传输，且 API 测试工具（curl/Python 大整数无损）测不出浏览器特有 bug。

## 4.5 文件互动（MinIO 双通道）

上传：前端 `multipart/form-data` → 后端 `MultipartFile` → MinIO `putObject`（私有桶）→ 库里只存 objectName。
读取：前端拿不到文件本体，只拿后端按权限生成的**短时效预签名 URL**（借阅剩多久 URL 活多久）→ `<img :src>` / `<a href>` 直接用。**防盗链的本质：访问凭证由后端按业务权限动态签发**。

---

# 第五部分 · 知识点全景清单

## 后端

- **Spring**：IoC/DI 三种注入方式、`@Transactional` 失效场景（自调用/非 public/异常被吞——项目里 StoryService 自调用踩过）、AOP 拦截器 vs 过滤器、`@RestControllerAdvice` 全局异常
- **MyBatis-Plus**：LambdaQueryWrapper 条件构造、`@TableLogic` 逻辑删除、`assign_id` 雪花算法（时间戳+机器位+序列）、分页插件
- **MySQL**：行锁/间隙锁、`UPDATE ... WHERE 条件` 的原子性、事务隔离级别（RR 下条件更新依然安全）、utf8mb4 与乱码、索引（status+userId 联合）
- **Redis**：SETNX+TTL 防死锁、ZSet 排行榜 `ZINCRBY/ZREVRANGE`、分布式锁的续期问题（看门狗）、缓存穿透/击穿/雪崩
- **JWT**：结构(header.payload.signature)、HS256 vs RS256、无状态 vs 黑名单 trade-off、**不要把敏感信息放 payload**（Base64 可解）
- **并发**：锁释放先于事务提交的竞态窗口、幂等设计（唯一键/防重 token）、金额用整型
- **工程**：多阶段 Dockerfile（Maven 构建→JRE 运行）、健康检查、环境变量注入配置、log-back 滚动

## 前端

- **Vue3**：Composition API、`ref` vs `reactive`、`computed` 缓存、生命周期（`onMounted` 拉数据）、组件通信（props/emits）
- **Vue Router**：懒加载 `() => import()`（按需 chunk）、history 模式与 Nginx `try_files` 配套、全局守卫
- **axios**：实例化与双拦截器、`Promise.reject` 中断链、统一错误分层
- **Vite**：dev proxy 原理（服务器端转发规避同源策略）、环境变量、构建产物 hash 命名与缓存
- **JS**：`Number.MAX_SAFE_INTEGER` 与大整数精度、`localStorage` vs cookie（XSS 面下 token 存储的 trade-off）、ESM

## 全栈互动

- 同源部署消灭 CORS / 开发期 proxy 转发
- 统一响应契约 R + 前端拆包，错误三层分工
- 前端守卫是体验、后端拦截器是安全
- "成功后重查询"保持 UI 与账本一致
- 雪花 ID 字符串化传输

---

# 第六部分 · 面试高频问答（30 题）

## A. 后端

**1. 为什么用 JWT 而不是 Session？**
分布式下 Session 要粘性或共享存储；JWT 自包含、免中心校验。代价是登出/封禁不能即时失效——本项目 72h 短期 + 每次校验 role 缓解，更严格场景加 Redis 黑名单（存未过期 token 的 jti，登出即写入）。

**2. @Transactional 什么时候失效？**
① 同类自调用（绕过代理，项目 StoryService 踩过，抽到另一个 bean 解决）；② 方法非 public；③ 默认只回滚 RuntimeException，受检异常要 `rollbackFor`；④ 异常被 try/catch 吞掉；⑤ 多线程内另开事务。

**3. 接单防超抢怎么做的？为什么锁内还要 DB 条件更新？**
Redis SETNX 锁（10s TTL）挡住并发流量，锁内校验等级/赤化/状态，最终以 `UPDATE task SET status=2 WHERE id=? AND status=1` 的 rows 判定归属。因为锁可能在事务提交前被释放（Redis 过期/手动 finally），DB 条件更新是最终防线——两层各司其职。

**4. 为什么 Redis 锁要设 TTL？finally 释放还不够吗？**
持锁进程崩溃时 finally 不会执行，无 TTL 就死锁。TTL 又引入"业务没做完锁先过期"问题 → 严格场景用 Redisson 看门狗自动续期。

**5. 余额扣减为什么不用"查出来减完再存回去"？**
两步之间无原子性，并发下丢失更新（超支）。用 `UPDATE ... SET gold=gold-? WHERE id=? AND gold>=?`，行锁内一条语句完成"检查+扣减"，rows=0 即余额不足。

**6. 金额字段为什么用 long 不用 Double？**
浮点二进制无法精确表示 0.1，累加有误差；分/金币天然整数，long 范围足够（9.2×10^18）。

**7. 月度结算怎么保证幂等？**
settlement_log 上 (month, settle_type) 唯一键：先 INSERT，冲突即本月已结算直接返回；成功后同事务写明细。重跑、并发触发都不会重复入账。

**8. 逻辑删除和物理删除怎么选？**
含审计/关联价值的（用户、任务、流水）逻辑删除；纯中间数据可物理删。MP `@TableLogic` 自动把 select 加 `deleted=0`、delete 变 update。

**9. 雪花 ID 的结构和坑？**
64 位 = 1 符号 + 41 时间戳 + 10 机器 + 12 序列，趋势递增对 B+ 树友好。坑：超出 JS 安全整数——后端 Long 统一 ToStringSerializer 序列化为字符串。

**10. BCrypt 和 MD5+盐的区别？**
BCrypt 自带盐并内嵌于密文、慢哈希（可调 cost）抗暴力破解；MD5 快，GPU 每秒数十亿次，必须自己管盐且依然弱。

**11. 缓存穿透/击穿/雪崩分别是什么，项目里怎么防？**
穿透：查不存在的数据绕过缓存打库 → 缓存空值/布隆过滤器；击穿：热点 key 过期瞬间打库 → 互斥锁重建；雪崩：大量 key 同时过期 → TTL 加随机。本项目排行榜 ZSet 常驻、接单用 SETNX 锁，天然规避了大头。

**12. 拦截器和过滤器的区别？**
Filter 是 Servlet 规范、在 DispatcherServlet 之前，拿不到 Handler 信息；Interceptor 是 Spring 机制、可注入容器 bean、能拿到 handler 注解做精细化放行（登录/文档接口白名单）。鉴权用 Interceptor 更合适。

**13. ThreadLocal 有什么风险？**
线程池复用线程，不清理会把上个请求的用户带到下个请求（串号越权）。必须在拦截器 `afterCompletion` 里 remove。

**14. 数据库连接池的原理？**
启动时建 N 个连接复用，避免每次 TCP+认证握手。HikariCP 默认 10 个，`maximumPoolSize` 按业务并发调，太小排队、太大拖垮库。

**15. Docker 多阶段构建解决什么问题？**
构建阶段用 Maven 镜像（含 JDK+依赖缓存），运行阶段只拷 jar 到精简 JRE 镜像——产物从 1GB+ 缩到 ~300MB，攻击面也小。

## B. 前端

**16. ref 和 reactive 的区别？**
ref 包装任意类型，通过 `.value` 访问，赋值整体替换安全；reactive 只用于对象，解构会失去响应性（要用 `toRefs`）。简单规则：基本类型用 ref，表单聚合对象可用 reactive。

**17. `<script setup>` 相比 Options API 的优势？**
逻辑按功能聚合而非按选项分散；顶层变量/函数自动暴露给模板；类型推导更好；编译后无组件实例代理开销。

**18. 路由懒加载为什么能优化首屏？**
`() => import('../views/X.vue')` 让每个页面打成独立 chunk，首屏只加载当前页 JS，其余按需拉取；配合 Vite 的 hash 文件名实现长缓存。

**19. axios 拦截器里怎么处理"HTTP 200 但业务失败"？**
响应拦截器判断 `body.code !== 0`：弹统一错误提示并 `return Promise.reject()`，让业务代码的 await 抛出、进入 catch；成功路径直接返回 `body.data`，页面代码零样板。

**20. 401 处理为什么放在拦截器而不是各页面？**
过期是全局横切关注点：拦截器里清 token、跳登录、弹提示一次搞定；页面里写要复制 N 份且容易漏。

**21. token 放 localStorage 还是 cookie？**
localStorage：不便携跨子域、但需自己防 XSS（不渲染用户输入的 HTML）；cookie+HttpOnly：防 XSS 读取但需防 CSRF（SameSite）。SPA 常用 localStorage + 严格 CSP；两者都可，能讲清 trade-off 即可。

**22. Vue Router 的 history 模式为什么刷新会 404？怎么解？**
history 模式靠 History API 伪造路径，服务器上并不存在 `/tasks` 文件 → Nginx `try_files $uri $uri/ /index.html` 把所有未知路径回退到 index.html 交给前端路由接管。

**23. 前端怎么防 XSS？**
Vue 模板默认转义插值；绝不 `v-html` 渲染用户输入；富文本用 DOMPurify 白名单过滤。

## C. 前后端互动

**24. CORS 是什么，这个项目为什么没出现？**
浏览器同源策略限制跨域 XHR。本项目**同源部署**（前端与 API 同域，Nginx 统一入口）从根上没有 CORS；开发期由 Vite proxy 做服务器端转发。若真要跨域：后端 `@CrossOrigin` 或 CorsFilter 配白名单。

**25. 描述一次"接单"从点击按钮到界面更新的全过程。**
（叙述 4.3 时序：按钮 → POST /tasks/{id}/accept → 反代 → JwtInterceptor → TaskService 双保险 → 返回结果/失败原因 → 拦截器拆包或弹错 → loadTasks 重查询刷新列表。要点：状态以后端为准。）

**26. 前后端的"契约"是什么？谁说了算？**
`R{code,message,data}` + 各接口 DTO schema（Knife4j 可导出）。后端是唯一事实源；前端 api 层按契约封装函数，页面永不手拼 URL。

**27. 表单校验前端做了，后端为什么还要做？**
前端校验只是体验（即时反馈），curl/Postman 可完全绕过。后端 `@Valid + @NotBlank` 才是安全边界——"永远不要信任客户端输入"。

**28. 前端如何保证显示的余额和数据库一致？**
不做本地累加，操作成功后**重查询**（`/adventurer/me`）。前端算术在并发/失败回滚场景必然漂移，重查询永远等于账本。

**29. 接口文档怎么维护才不会烂？**
注解即文档：`@Operation/@Tag` 写在代码里，Knife4j 启动自动生成——代码改了文档必改，不会出现"文档落后三个版本"。

**30. 如果让你把接单改成"10 万人同时抢"，方案怎么演进？**
① Redis 锁粒度已经是 per-task；② 把"状态判断+扣减"挪进 Redis（DECR/位图预占）异步落库削峰；③ MQ 排队串行化写库；④ 前端摇号/排队动画减少无效重试；⑤ 库存维度分片。核心思路：**逐层把冲突从 DB 前移到内存**。

---

# 附 · 自我介绍项目一句话模板

> "我独立开发了一个全栈游戏项目：后端 SpringBoot 3 + MyBatis-Plus，25 张表支撑三协会经济体系，重点解决了**并发接单（Redis 锁 + DB 条件更新双保险）、乐观扣款防超支、月度结算幂等、MinIO 签名 URL 防盗链、雪花 ID 前端精度丢失**等问题；前端 Vue3 + Element Plus，axios 双拦截器统一鉴权与错误处理；配套单测/接口回归/浏览器 E2E 三层验证与 GitHub Actions CI，docker-compose 一键部署。"
