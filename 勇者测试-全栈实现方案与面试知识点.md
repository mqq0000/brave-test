# 勇者测试 · 全栈实现方案与面试知识点（讲透版）

> 基于仓库真实代码（`mqq0000/brave-test`）逐模块撰写：后端 29 张表 / 11 个 Service / 40+ 接口，前端 8 页面。全部经过 13 单测 + 38 API 回归 + 12 浏览器 E2E 三层验证。

---

# 第一部分 · 项目是什么：一句话与一张图

**一句话**：一个"文字冒险 + 性格养成 + 自我激励"的全栈网页游戏——高污染世界中，玩家通过做好事（善事任务）、做对选择（剧情）、兑现自我承诺（修身任务）来净化自己的萌芽，同时在三协会经济体系中赚金币、升级、共享知识。

**全景拓扑**：

```
┌──────────── 浏览器（Vue3 SPA, 8 页面）────────────┐
│  Login  Layout  TaskHall  Shop  Knowledge        │
│  Story  SelfMotivation  Profile  Admin           │
└──────────────┬────────────────────────────────────┘
               │ axios（baseURL=/api/v1，请求拦截器自动带 Bearer token）
┌──────────────▼────────────────────────────────────┐
│  开发期: Vite proxy(/api→8080)  生产期: Nginx:80   │
└──────────────┬────────────────────────────────────┘
┌──────────────▼────────────────────────────────────┐
│  SpringBoot 3.3.4 (:8080)                          │
│  JwtInterceptor(验签+黑名单) → UserContext         │
│  Controller(11) → Service(11) → Mapper(29 实体)    │
└───┬──────────────┬───────────────┬────────────────┘
    ▼              ▼               ▼
 MySQL8:29表    Redis7          MinIO
 (业务数据/     (JWT黑名单/     (私有桶+
  事务/对账)     接单锁/ZSet      预签名URL)
                排行榜)
```

---

# 第二部分 · 数据库设计全解（29 张表按 7 域）

## 2.1 用户域（2 表）

| 表 | 关键字段 | 说明 |
|---|---|---|
| `user` | username(唯一)、password(BCrypt密文)、role、status | role: ADVENTURER / 四种 ADMIN；status=1 禁用 |
| `adventurer` | user_id、rank_level(D~S)、completed_count、pollution_value、sprout_status、gold_balance、knowledge_banned、banned_until | 一人一档案；**sprout_status 不落"计算"而是每次污染变化时由 `deriveSproutStatus()` 推导后写回**（0~39 NORMAL / 40~69 GRAY / 70~100 RED），查询零计算 |

**面试点**：为什么 `sprout_status` 冗余存储而不是查询时算？——读多写少，避免每次列表页重复计算；代价是写时要保证两字段同事务更新。

## 2.2 任务域（4 表）

| 表 | 说明 |
|---|---|
| `task` | source_type(1行者/2冒险者)、status(**0待审/1开放/2进行/3待验收/4完成/5失败/6驳回/7下架** 状态机落库)、task_level(D~S)、reward_gold、acceptor_gold(**落库时就算好 90%**，验收时直接发)、purify_value、publisher_id、acceptor_id、audit_id/audit_remark |
| `task_accept_record` | 每次接单一条：is_cross_level、status(0进行/1成功/2失败)、gold_paid、purify_gained——**验收结算的依据**，与 task 双写 |
| `xingzhe_apply` | 行者帮助申请：applicant、audit_status(0待/1过/2驳)、published_task_id（审批通过后回链生成的任务） |
| `purification_record` | 污染/净化流水：source、value_before/after、ref_id——污染值同样可对账 |

## 2.3 商城域（3 表）

`shop_item`（净化剂等商品）、`purification_batch`（总管理员月度生产批次：batch_month + 数量 → **月限 100 支的依据**）、`trade_order`（订单，购买扣款+库存扣减同事务）。

## 2.4 知识宝库域（6 表）

| 表 | 说明 |
|---|---|
| `knowledge_contribution` | 投稿原文与状态（待封装/已封装） |
| `info_set` | 封装后的商品：title、price、status(1在售/2下架)、minio_object(正文在对象存储，库里只存指针) |
| `borrow_record` | 借阅：card_type(1日/2周/3月)、expire_at、status |
| `purchase_record` | 买断记录 |
| `violation_record` | 管理员处置记录（踢出知识宝库 → adventurer.knowledge_banned=1） |
| `violation_report` / `knowledge_favorite` | 用户举报闭环（0待处理/1已处罚/2已驳回）与收藏（**user_id+info_set_id 唯一键**，幂等切换的数据库保障） |

## 2.5 修身域（4 表）

| 表 | 说明 |
|---|---|
| `self_task` | 自建任务：coin_reward(1~100)、repeat_type(0一次性/1每日)、status、last_completed_at、**streak_days(连续打卡)** |
| `skill` | 技能树：parent_id(NULL=根)、exp；等级由 `level()` 方法推导（每 100 经验 1 级，上限 9） |
| `self_shop_item` | 自建现实奖励商品 |
| `self_redemption` | 兑换记录：cost + **assoc_fee(10%)** ——与协会经济接轨的凭证 |

## 2.6 财务域（3 表）——整个系统的"账本心脏"

| 表 | 说明 |
|---|---|
| `gold_flow` | 每笔金币变动：biz_type(TASK_REWARD/ESCROW/REFUND/SELF_TASK/SKILL_BRANCH/SELF_REDEEM/LEVEL_UP_REWARD…)、amount(±)、**balance_after(余额快照)**、ref_type+ref_id(业务溯源) |
| `association_ledger` | 三协会+总管理员按月账本：association(1行者/2冒险者/3知识/4总管)、month、income/expense/balance |
| `settlement_log` | 月度结算幂等键：(month, settle_type) 唯一 |

**对账公式**：`SUM(gold_flow.amount) == 当前 gold_balance - 初始余额`。任何一笔钱都能通过 ref 溯源到具体业务单据。

## 2.7 内容域（5 表）

`story_chapter` → `story_node`（章节内节点，seq 排序）→ `story_option`（选项 JSON：text、gold_delta、pollution_delta、next_node_id）→ `player_story_progress`（每用户每章节：current_node_id、status、**choices_log(选择轨迹 JSON)**）；`test_question`/`test_result`（保留的测试模块，前端入口已移除）。

---

# 第三部分 · 后端逐模块讲透（真实代码 + 讲解）

## 3.1 基础设施：统一响应 / 异常 / 用户上下文

```java
// R.java —— 前后端唯一的响应契约
public class R<T> { int code; String message; T data; }   // code=0 成功

// UserContext.java —— ThreadLocal 用户上下文
public class UserContext {
    private static final ThreadLocal<Long> USER = new ThreadLocal<>();
    private static final ThreadLocal<String> ROLE = new ThreadLocal<>();
    public static Long getUserId() { return USER.get(); }   // Controller/Service 随取随用
}
```

`GlobalExceptionHandler` 三层兜底：`BusinessException`（业务，code 原样）/ `MethodArgumentNotValidException`（@Valid 失败 400+字段提示）/ `Exception`（500"系统繁忙"，**堆栈只进日志不进响应**）。

## 3.2 认证 AuthService（注册双表事务 + 黑名单登出）

```java
@Transactional
public Long register(RegisterDTO dto) {
    if (userMapper.selectCount(new LambdaQueryWrapper<User>()
            .eq(User::getUsername, dto.getUsername())) > 0)
        throw new BusinessException("用户名已被占用");
    User user = new User();
    user.setPassword(passwordEncoder.encode(dto.getPassword()));  // BCrypt，绝不明文
    user.setRole("ADVENTURER");
    userMapper.insert(user);
    adventurerMapper.insert(初始档案: D级/污染0/金币0);            // 同一事务，不会出现"有账号无档案"
}

public void logout(String token) {
    Claims claims = jwtUtil.parse(token);
    long remainMs = claims.getExpiration().getTime() - System.currentTimeMillis();
    if (remainMs > 0)
        redisTemplate.opsForValue().set("jwt:blacklist:" + token, "1",
                Duration.ofMillis(remainMs));                      // TTL = token 剩余寿命
}
```

**JwtInterceptor 每次请求**：`Bearer ` 前缀校验 → 验签 → **Redis 黑名单检查** → 写 UserContext → `afterCompletion` remove。

**面试点**：这是"JWT 无状态 + Redis 黑名单"的**混合方案**——平时零库查（只有 Redis GET），登出/封禁可即时失效，TTL 与 token 同寿命自动清理。被问"JWT 怎么登出"时直接答这套。

## 3.3 财务 FinanceService（一行 SQL 的原子扣款）

```java
@Transactional
public void deductGold(Long adventurerId, long amount, ...) {
    int rows = adventurerMapper.update(null, new LambdaUpdateWrapper<Adventurer>()
            .eq(Adventurer::getId, adventurerId)
            .ge(Adventurer::getGoldBalance, amount)                 // 余额够才扣
            .setSql("gold_balance = gold_balance - " + amount));
    if (rows == 0) throw new BusinessException("金币不足");
    Adventurer adv = adventurerMapper.selectById(adventurerId);
    saveFlow(adv.getUserId(), -amount, adv.getGoldBalance(), ...);  // 流水带快照
}
```

**为什么这不是"先查后扣"**：`WHERE balance >= ?` 和 `SET balance = balance - ?` 在**同一条 UPDATE 的行锁内**原子完成"检查+扣减"，并发下不可能超支；`rows==0` 即余额不足，无需 SELECT。流水 `balance_after` 让任何时刻可对账。

`ledgerDelta()`：协会月账本不存在则初始化、存在则累加——三协会所有收入（佣金 10%、开枝 50 币、兑换 10% 会费）统一从这里入账。

## 3.4 冒险者 AdventurerService（萌芽状态机 + ZSet 排行榜）

```java
public static String deriveSproutStatus(int pollution) {
    if (pollution >= 70) return "RED";      // 赤化：禁接 S 级
    if (pollution >= 40) return "GRAY";     // 灰化：警示
    return "NORMAL";
}
// raisePollution / purify：更新污染值+推导状态+写 purification_record 流水，同一事务

// 排行榜：完成任务数 ZSet
redisTemplate.opsForZSet().add("rank:completed", userId, completedCount);
// Top N：reverseRangeWithScores —— O(logN) 插入、O(logN) 查询，天然排序
```

**面试点**：为什么用 ZSet 不用 SQL `ORDER BY completed_count LIMIT 10`？——数据量小时都行；ZSet 的价值在**写时 O(logN) 维护有序**，读 Top N 是 O(logN)，且不占 MySQL 连接，高并发榜单场景数据库零压力。

## 3.5 任务中心 TaskService（最核心的状态机 + 并发控制）

**任务状态机**：

```
行者任务:  [发布] ──► 1 开放 ──► 2 进行 ──► 3 待验收 ──┬─► 4 完成
冒险者任务: 0 待审 ──┬─► 1 开放                       └─► 5 失败
                    └─► 6 驳回(退托管)
```

### 发布（托管制）

```java
task.setAcceptorGold(Math.round(rewardGold * (1 - COMMISSION_RATE)));  // 90% 落库算好
task.setStatus(ST_PENDING_REVIEW);
taskMapper.insert(task);
financeService.deductGold(adv.getId(), rewardGold, "ESCROW", "TASK", task.getId()); // 全额托管
```

**为什么托管**：防止接单人完成后发布者没钱付；审核驳回/验收失败按 90% 退回（10% 佣金不退，协会已承担审核成本）。

### 接单（双保险，见代码与逐行讲解）

```java
public Long acceptTask(Long userId, Long taskId, boolean confirmCrossLevel) {
    String lockKey = ACCEPT_LOCK_PREFIX + taskId;
    Boolean locked = redisTemplate.opsForValue().setIfAbsent(lockKey, "1", Duration.ofSeconds(10));
    if (!Boolean.TRUE.equals(locked)) throw new BusinessException("任务正在被其他冒险者接取…");
    try {
        Adventurer adv = financeService.requireAdventurerByUserId(userId);
        checkNotBanned(adv);                                    // 越级失败封禁期校验
        Task task = taskMapper.selectById(taskId);
        if (task.getStatus() != ST_OPEN) throw new BusinessException("任务当前不可接取");
        if ("RED".equals(adv.getSproutStatus()) && "S".equalsIgnoreCase(task.getTaskLevel()))
            throw new BusinessException("萌芽赤化，无法接取 S 级任务…");   // 世界观规则落进代码
        // 越级：最多越 2 级，且必须前端显式传 confirmCrossLevel=true（风险知情确认）
        ...
        int rows = taskMapper.update(null, new LambdaUpdateWrapper<Task>()
                .eq(Task::getId, taskId).eq(Task::getStatus, ST_OPEN)   // ★ 条件更新
                .set(Task::getStatus, ST_IN_PROGRESS).set(Task::getAcceptorId, userId));
        if (rows == 0) throw new BusinessException("手慢了，任务已被其他冒险者接取");
        acceptRecordMapper.insert(record);                       // 接单记录（验收依据）
        return record.getId();
    } finally {
        redisTemplate.delete(lockKey);
    }
}
```

**两层各司其职**（面试高光）：
- **Redis 锁**：把 1000 个并发请求变成 1 个进业务、999 个立刻收到"请稍后"——削流量、省 DB 连接；
- **DB 条件更新**：`WHERE status=1` 在 InnoDB 行锁下保证**只有一个人能把 1 改成 2**——即使 Redis 锁失效（过期/崩溃），数据库层面依然不会一单多人。
- 已知妥协写在 Javadoc 里：锁在事务提交前 finally 释放存在极小竞态窗口，正是因此第二层才必要。

### 验收（成功 = 四笔账一个事务）

```java
// success 分支：
financeService.addGold(acceptor.getId(), task.getAcceptorGold(), ...);   // ① 发 90% 报酬
adventurerService.purify(acceptor.getId(), purify, ...);                 // ② 净化萌芽
// ③ 完成计数+1；越级成功直升任务等级；否则每 100 件自然升级 +1 级奖励 1 万
financeService.ledgerDelta(ASSOC_ADVENTURER, month, commission, 0);      // ④ 10% 佣金入协会
adventurerService.recordRank(userId, newCount);                          // ⑤ ZSet 排行榜
// failure 分支：越级失败 → banned_until = now + 30 天；退还发布者 90%
```

## 3.6 知识宝库 KnowledgeService（六表闭环）

```
投稿(contribution) → 管理员封装定价(info_set, 正文入 MinIO)
    → 浏览(搜索: LIKE 标题) / 收藏(favorite, 唯一键幂等)
    → 借阅(borrow: 日10%/周25%/月50% of price) / 购买(purchase: 100%)
    → 续费(renew: 从 expire_at 顺延，非从当前时间！)
    → 举报(report, 禁自举报) → 管理员 handle(penalize=true → knowledge_banned=1)
```

关键规则：**借阅有效期与签名 URL 时效联动**——`getPresignedObjectUrl(expiry = borrowRemainSeconds)`，借阅到期那刻 URL 同步失效，无需任何定时任务；40% 营利入知识协会账本；`knowledge_banned=1` 的用户在所有知识接口入口被 `requireNotKnowledgeBanned()` 拦截（403"永久失去进入知识宝库的权利"）。

## 3.7 剧情引擎 StoryService（数据驱动的状态图）

```
story_chapter(seq 解锁链) → story_node(seq) → story_option(gold_delta, pollution_delta, next_node_id)
                                ↑ player_story_progress(current_node_id, status, choices_log)
```

`choose()` 的结算逻辑：选项的 `gold_delta > 0` 加币、`pollution_delta` 正负决定染污/净化、`next_node_id == null` 即本章完结（progress.status→1，解锁下一章）。**剧情内容 100% 数据驱动**——加新章节只需 INSERT 三张表，零代码发布。

**踩过的坑（面试素材）**：早期在 Service 内 `this.choose()` 自调用导致 `@Transactional` 失效（Spring AOP 代理绕过），重构为跨 Service 调用解决。

## 3.8 修身 SelfMotivationService（自我激励经济学）

| 规则 | 实现 |
|---|---|
| 单次奖励 1~100 币 | `createTask` 入参校验 |
| 铸币日上限 500 | **不做计数器缓存，直接查当日 `gold_flow` 中 `biz_type='SELF_TASK'` 入账之和**——流水即真相，零额外状态 |
| 每日防重复 | `lastCompletedAt.toLocalDate() == today` 拒绝 |
| 连续打卡 | 昨天（东八区）完成过 → streak+1，否则重置 1；奖励 `+5×(streak-1)` 封顶 +50 |
| 技能经验 | 完成关联任务 +20 exp，100/级上限 9 级，升级即时返回 `levelUp=true` |
| 开枝 50 币 | `deductGold + ledgerDelta(冒险者协会)` 同事务 |
| 兑换 10% 会费 | `deductGold(cost) + ledgerDelta(cost*0.1)` 同事务 |
| 净化萌芽 | 每次完成 `purify(2)` |

**时区坑**：容器默认 UTC，北京时间 0~8 点"今天"会算错——代码层 `ZoneId.of("Asia/Shanghai")` + 容器层 `TZ` 环境变量双保险。

## 3.9 月度结算 SettlementService（幂等）

`settlement_log` 的 `(month, settle_type)` 唯一键：Job 先 INSERT 该键，插成功才执行结算，冲突直接跳过。佣金 60% 月初上缴总管理员等规则都在结算事务内完成。

## 3.10 MinioService（签名 URL + host 重写）

```java
String url = minioClient.getPresignedObjectUrl(...expiry(seconds)...);
// 容器内签名 host 是 minio:9000，浏览器打不开 → 用 public-endpoint 配置重写：
url = url.replace(minioInternalHost, publicEndpoint);
```

---

# 第四部分 · 前端逐层讲透

## 4.1 main.js —— 应用装配

```js
createApp(App).use(ElementPlus).use(createPinia())
  // 全局注册全部图标：<el-icon><Share /></el-icon> 任何地方直接用
  .use(router).mount('#app')
```

## 4.2 request.js —— 双拦截器（全文精讲）

```js
const request = axios.create({ baseURL: '/api/v1', timeout: 15000 })

// 请求拦截：唯一职责——塞 token
request.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = 'Bearer ' + token
  return config
})

// 响应拦截：拆 R 包装 + 三层错误分工
request.interceptors.response.use(
  (resp) => {
    const body = resp.data
    if (body.code !== 0) {                        // HTTP 200 但业务失败
      ElMessage.error(body.message || '请求失败')
      return Promise.reject(new Error(body.message))  // 让 await 抛出，页面可 catch
    }
    return body.data                              // ★ 直接解包，页面少一层 .data
  },
  (error) => {
    if (error.response?.status === 401) {         // token 失效 → 全局登出
      localStorage.removeItem('token'); router.push('/login')
      ElMessage.error('登录已过期，请重新登录')
    } else {
      ElMessage.error(error.response?.data?.message || '网络异常，请稍后再试')
    }
    return Promise.reject(error)
  }
)
```

**三层错误分工**：HTTP 层（401/500）→ 拦截器；业务层（code≠0）→ 拦截器弹 toast + reject；页面层 → 只写成功路径 `try/catch`。**全站 8 个页面零重复错误处理代码**。

## 4.3 router —— 守卫即体验，后端才是安全

```js
router.beforeEach((to) => {
  if (to.name !== 'login' && !localStorage.getItem('token')) return { name: 'login' }
  if (to.name === 'admin' && !ADMINS.includes(localStorage.getItem('role') || ''))
    return { name: 'tasks' }     // 非管理员重定向
  return true
})
```

路由懒加载 `() => import('../views/X.vue')` 每页独立 chunk。**面试要点**：改 localStorage 伪造 role 可进 admin 页面，但所有 admin 接口在后端 `requireRole()` 二次校验，curl 直打也是 403——前端守卫防"误入"，后端防"入侵"。

## 4.4 页面模式举例（SelfMotivation.vue）

```js
const goldBalance = ref(0)
const skillTotalLevel = computed(() => skills.value.reduce((s, x) => s + (x.level || 0), 0))

async function doComplete(t) {
  const res = await completeSelfTask(t.id)      // 后端返回本次全部收益明细
  let msg = `获得 ${res.reward} 修行币`
  if (res.streakBonus > 0) msg += `（含连续打卡奖励 +${res.streakBonus}）`
  if (res.skillName) msg += `，「${res.skillName}」+20 经验` + (res.levelUp ? '，升级！' : '')
  ElMessage.success(msg + '，萌芽净化 +2')
  loadTasks(); loadBalance()                    // 重查询刷新，UI 永远等于账本
}
```

顶部三张统计卡（💰修行币 / 🌱污染值 / 🌿技能总等级）数据来自 `/adventurer/me` + 技能树聚合，任何增减币操作后自动刷新。

---

# 第五部分 · 前后端互动全过程（四个可背的时序）

## 5.1 登录（认证时序）

```
Login.vue 提交 → POST /auth/login {username,password}
→ AuthService: BCrypt.matches 校验密文 / status 禁用检查
→ JwtUtil.create(userId, username, role) 签发 72h token
→ 前端 localStorage 存 token/role → 跳 /tasks
→ 此后每个请求经请求拦截器自动携带 Authorization: Bearer xxx
→ JwtInterceptor: 验签 → Redis 黑名单 → UserContext(ThreadLocal)
```

## 5.2 任务全生命周期（状态机时序，最能体现"互动即状态同步"）

```
发布者前端填表 → POST /adventurer/tasks
  后端: 校验等级报酬区间 → 建 task(status=0) → 托管扣款(ESCROW 流水)
管理员后台 → GET /admin/tasks/pending → POST /admin/tasks/{id}/audit
  后端: 通过 → status=1（大厅可见）；驳回 → status=6 + 退托管
接取者大厅点击接单 → POST /tasks/{id}/accept?confirmCrossLevel=
  后端: Redis 锁 → 等级/赤化/封禁校验 → 条件更新占单 → 写 accept_record
  前端: 失败弹 toast 并刷新列表（状态以后端为准）
接取者完成 → POST /tasks/{id}/submit  → status=3
发布者/管理员 → POST /tasks/{id}/verify?success=
  后端一个事务: 发90%报酬 + 净化 + 升级判定(直升/100件) + 佣金10%入协会 + ZSet 榜
  或: 越级失败封禁30天 + 退发布者90%
```

## 5.3 知识宝库借阅→到期→续费

```
借阅 POST /info-sets/{id}/borrow?cardType=3
  后端事务: 按价 50% 扣款 → borrow_record(expire_at=now+30天) → 40% 入知识协会
阅读 GET /info-sets/{id}/content
  后端: 校验"已购或借阅未过期" → 按剩余秒数签 MinIO URL → 前端 <a href> 直接打开
续费 POST /borrows/{id}/renew?cardType=2
  后端: expire_at = 原到期时间 + 7 天（未到期续费不损失剩余天数）
```

## 5.4 举报闭环（用户生成内容的安全互动）

```
用户点举报 → POST /knowledge/reports?offenderId&infoSetId&description（禁自举报）
knowledge_admin 后台 → GET /admin/knowledge/reports/pending
  → POST /admin/knowledge/reports/{id}/handle?penalize=true&remark=
     penalize=true: 复用 reportViolation → offender.knowledge_banned=1（同事务）
     penalize=false: 驳回 + handle_remark 留痕
```

## 5.5 两个真实踩坑（比八股更有说服力）

1. **雪花 ID 精度丢失**：curl 全绿、浏览器购买报"不存在或已下架"。根因 19 位 id 超出 `Number.MAX_SAFE_INTEGER`，`JSON.parse` 静默舍入尾数。修复：`JacksonConfig` 把 Long 统一序列化为字符串，Spring 自动转回。**教训**：雪花 id 项目必须约定字符串传输；纯后端测试工具测不出浏览器特有 bug。
2. **构建错误被管道吞掉**：`docker compose build | tail` 吞了 Maven 编译失败（少 import），容器一直跑旧镜像。修复后固化规范：**构建必须看 exit code，部署后必须验证产物**（容器内 grep class 文件方法名）。

---

# 第六部分 · 知识点全景速查

**后端**：IoC/DI · `@Transactional` 失效五场景（自调用/非public/受检异常/异常被吞/多线程）· 拦截器 vs 过滤器 · ThreadLocal 与线程池 · BCrypt 慢哈希 · JWT 结构与混合黑名单 · MyBatis-Plus 条件构造/逻辑删除/雪花ID · InnoDB 行锁与条件更新原子性 · 事务隔离级别 · Redis SETNX+TTL/看门狗/ZSet/穿透击穿雪崩 · 幂等设计（唯一键） · 金额整型 · 多阶段 Dockerfile · 健康检查 · CI 门禁

**前端**：Composition API · ref vs reactive · computed 缓存 · 路由懒加载+history 模式+Nginx try_files · 全局守卫 · axios 拦截器与 Promise.reject 链 · Vite proxy 原理 · token 存储权衡（localStorage vs HttpOnly cookie，XSS/CSRF） · `Number.MAX_SAFE_INTEGER` · ESM

**全栈**：同源部署消灭 CORS · R 契约与拆包 · 前端校验是体验后端校验是安全 · 重查询保一致 · 签名 URL 按权限动态签发 · 雪花 ID 字符串化

---

# 第七部分 · 面试问答 40 题（含追问链）

## A. 后端基础（15）

1. **为什么 JWT 而不是 Session？** 分布式免共享存储；但登出不能即时失效。追问"那你登出怎么办"→ 项目实际实现了 **Redis 黑名单混合方案**：登出时按 token 剩余 TTL 写 `jwt:blacklist:{token}`，拦截器先查黑名单——平时零 MySQL 开销，又能即时踢人。
2. **@Transactional 什么时候失效？** 自调用绕过代理（项目 StoryService 踩过，重构跨 Service 解决）/ 非 public / 默认不回滚受检异常 / catch 吞异常 / 新线程。追问"自调用为什么失效"→ Spring 事务靠 AOP 代理，this 调用不走代理对象。
3. **接单防超抢两层各干什么？** Redis SETNX（10s TTL）削并发流量；DB `WHERE status=1` 条件更新在行锁内保证唯一占单。追问"锁过期了怎么办"→ 正因为锁可能提前失效，DB 层才是最终防线，两层缺一不可。
4. **Redis 锁为什么要 TTL？** 持锁进程崩溃 finally 不执行会死锁；TTL 又带来业务未完成锁先过期 → 严格场景 Redisson 看门狗续期。
5. **扣款为什么一条 UPDATE？** `WHERE balance>=? SET balance=balance-?` 在行锁内原子完成"检查+扣减"，并发不超支；先 SELECT 再 UPDATE 有丢失更新窗口。
6. **金额为什么 long？** 浮点误差；金币天然整数。
7. **结算幂等怎么做？** settlement_log (month,type) 唯一键，先 INSERT 抢幂等权，冲突跳过。
8. **流水表为什么要余额快照？** `SUM(amount)` 与快照链双重对账，任何一笔钱可溯源（ref_type+ref_id）到业务单据。
9. **逻辑删除 vs 物理删除？** 审计价值用 `@TableLogic`；查询自动带 `deleted=0`。
10. **雪花 ID 结构与坑？** 41 时间戳+10 机器+12 序列；趋势递增利索引；**JS 精度丢失** → 后端统一序列化字符串。
11. **BCrypt 为什么安全？** 慢哈希+内置盐；GPU 抗性。
12. **排行榜为什么 ZSet？** 写时 O(logN) 维护有序，读 TopN O(logN)，MySQL 零压力。
13. **拦截器 vs 过滤器？** Servlet 规范 vs Spring MVC；后者能拿 handler 做注解级放行；鉴权用拦截器。
14. **ThreadLocal 风险？** 线程池复用串号，`afterCompletion` 必须 remove。
15. **多阶段 Dockerfile？** 构建层 Maven/JDK，运行层仅 JRE+jar，产物 ~300MB。

## B. 前端（10）

16. **ref vs reactive？** ref 任意类型 `.value`；reactive 对象专用、解构失响应（toRefs 补救）。
17. **`<script setup>` 优势？** 逻辑聚合/自动暴露/更好类型推导/更小编译产物。
18. **路由懒加载原理？** 动态 import 按 chunk 切分，首屏只载当前页。
19. **HTTP 200 但 code≠0 怎么处理？** 响应拦截器统一弹 toast 并 reject，成功路径直接返回 data——页面零样板。
20. **401 全局处理放哪？** 拦截器：清 token → 跳登录 → 提示，一处搞定。
21. **token 存哪？** localStorage（防 CSRF 强、需防 XSS）vs HttpOnly cookie（防 XSS 读、需 SameSite 防 CSRF），能讲清 trade-off 即可。
22. **history 模式刷新 404？** `try_files ... /index.html` 回退给前端路由。
23. **前端 XSS 防护？** 模板默认转义；禁 v-html 渲染用户输入。
24. **computed 和 methods 区别？** computed 有依赖缓存，依赖不变不重算。
25. **为什么操作成功后重查询而不是前端改本地状态？** 并发/回滚下本地推算必漂移，重查询恒等于账本。

## C. 前后端互动（9）

26. **CORS 为什么没出现？** 同源部署；开发期 Vite proxy 是服务器端转发，同源策略只约束浏览器。追问"如果前后端必须分域"→ 后端 CorsFilter 白名单。
27. **画出接单请求全链路。** 按钮 → axios(拦截器加 header) → 反代 → JwtInterceptor → TaskService 双保险 → R 响应 → 拦截器拆包/弹错 → 重查询刷新。
28. **前后端契约是什么？** `R{code,message,data}` + DTO schema（Knife4j 注解即文档）；后端是唯一事实源。
29. **前端校验了为什么后端还要校验？** curl 可绕过前端；`@Valid` 是安全边界——"永不信任客户端"。
30. **登录后 token 放哪、每次怎么带？** localStorage；请求拦截器统一 `Bearer `。
31. **过期 token 的请求后端怎么表现？** JwtInterceptor 抛 401 → 全局异常 → 前端拦截器捕获 401 → 清库跳登录。
32. **文件怎么传怎么读？** multipart 上传 → MinIO 私有桶（库存 objectName）；读取按业务权限签短时效预签名 URL。
33. **怎么防止下载链接被盗？** URL 时效 = 业务权限时效（借阅剩余秒数）；到期即失效，无需定时任务。
34. **接口幂等怎么保证（前端重复点击）？** 后端状态机/唯一键兜底 + 前端 loading 防抖，两端都做。

## D. 系统设计与演进（6）

35. **任务状态机为什么放数据库而不是内存？** 服务重启不丢、多实例一致、可审计。
36. **10 万人抢单怎么演进？** Redis 预占（DECR）→ MQ 串行落库 → 库存分片 → 前端排队；核心是把冲突从 DB 前移到内存。
37. **如果加"拍卖行"功能，改动哪些层？** 新表 → entity/mapper → Service → Controller → api/index.js 函数 → 页面；照此项目模式约半天，演示工程化收益。
38. **缓存一致性怎么处理？** 本项目排行榜用 ZSet 直写（写穿）；若加读缓存用"先更库后删缓存 + 短 TTL"。
39. **为什么每次改动都跑三层测试？** 单测抓逻辑、API 回归抓契约、浏览器 E2E 抓真实用户路径——雪花 ID bug 只有第三层能抓到（真实案例）。
40. **这个项目最难的 bug？** 讲大整数精度丢失完整故事：现象差异（curl 绿/浏览器挂）→ 定位（对比 id 字符串）→ 修复（Jackson 序列化）→ 防回归（约定+文档）。展示的是排查方法论。

---

# 附 · 30 秒项目介绍（可直接背）

> "我独立开发了'勇者测试'——一个文字冒险加自我激励的全栈游戏。后端 SpringBoot 3 + MyBatis-Plus，29 张表支撑三协会经济体系，我重点解决了并发接单（Redis 锁加数据库条件更新双保险）、乐观扣款防超支、月度结算幂等这三个资金安全问题；前端 Vue3 + Element Plus，用 axios 双拦截器统一了鉴权和错误处理。整个项目有 JUnit 单测、38 例接口回归、12 例浏览器 E2E 三层验证，GitHub Actions 持续集成，docker compose 一键部署。开发中我还踩过并解决了雪花 ID 导致的前端大整数精度丢失这类很典型的全栈问题。"
