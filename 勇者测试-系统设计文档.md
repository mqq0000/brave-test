# 《勇者测试》系统设计文档

> 版本：v1.0 ｜ 日期：2026-09-12 ｜ 技术栈：SpringBoot 3.x + MyBatis-Plus + MySQL8 + Redis + JWT + MinIO（可选 RabbitMQ），前端 Vue3 + Element Plus，接口文档 Knife4j

---

## 1. 项目定位与核心立意

**核心立意**：学会辨别负能量，守住内心勇者萌芽，相信自身力量；众人同心共建积极美好的世界。人是社会的产物，当世界向着美好前行，每个人都会变得美好。

**世界观**：高污染环境下，每位拥有"勇者萌芽"的冒险人都在被污染侵蚀——萌芽变灰（沮丧抑郁）或变红（愤怒暴戾），身体沉重、丧失斗气，社会高压弥漫。三大协会（行者协会、冒险者协会、知识宝库）构成了冒险者净化自我、互助成长、共建美好世界的核心舞台。

**玩法闭环**：
`接任务（善行/委托/知识分享） → 获得金币 + 净化污染 → 萌芽恢复 → 升级/购买净化剂 → 帮助他人 → 世界污染度下降`

---

## 2. 核心规则形式化

### 2.1 冒险者与萌芽（污染系统）

| 概念 | 规则 |
|---|---|
| 萌芽状态 | `正常 NORMAL`（污染值 0~39）、`灰化 GRAY`（40~69，抑郁沮丧倾向）、`赤化 RED`（70~100，愤怒暴戾倾向） |
| 污染值 | 整数 0~100，随剧情推进与时间自然增长（可配置日增量，建议 +1~3） |
| 净化途径 | ① 完成行者协会善事任务：-10/次（可按任务难度配置）② 购买并使用净化剂：-50/支 ③ 贡献独有知识被收录：-5/条 |
| 负面表现 | 污染值影响界面色调、文案倾向与可选剧情分支；GRAY 阶段任务收益 -10%，RED 阶段禁止接取 S 级任务（强化"辨别负能量"的教学含义） |

### 2.2 任务等级与金币体系

| 等级 | 金币区间（任务报酬） | 备注 |
|---|---|---|
| D | 80 ~ 120 | 初始等级 |
| C | 800 ~ 1200 | |
| B | 8,000 ~ 12,000 | |
| A | 80,000 ~ 120,000 | |
| S | 120,000 以上（上不封顶） | 仅官方/总管理员与高等级冒险者可发布 |

### 2.3 冒险者升降级规则

| 规则 | 说明 |
|---|---|
| 初始等级 | D |
| 自然升级 | 每完成 **100 件**任务自然升一级（D→C→B→A→S） |
| 越级晋升 | 接取**高于**自身等级的任务：**成功 → 直接升至该任务等级**；**失败 → 冒险者协会对其暂停开放 1 个月**（封禁接单/发布，`banned_until = now + 30d`） |
| 升级奖励 | 每升一级，协会奖励 **10,000 金币**（系统发放，计入协会账本支出） |
| 接单权限 | 只能接取 ≤ 自身等级的任务 + 至多跨 2 级的越级挑战任务（避免 D 级直接刷 S 级自杀式接单，越级需二次确认）※ 此条为设计建议，可按需开关 |

### 2.4 冒险者协会佣金规则

| 规则 | 说明 |
|---|---|
| 发布任务 | 冒险者发布任务需**预付全额金币**（托管到协会账户） |
| 抽成 | 协会收取发布者报酬的 **10%**，接取者完成任务实际到手 **90%** |
| 上缴 | 每月初，将上月总佣金的 **60%** 上缴总管理员，40% 留存协会运营 |

### 2.5 特殊商品：净化剂

| 规则 | 说明 |
|---|---|
| 生产 | 仅**总管理员**可生产，**每月限 100 支** |
| 定价 | **50,000 金币/支**（统一定价，不可改价） |
| 流通 | 总管理员 → 冒险者协会管理员（供货）→ 冒险者商城上架 → 冒险者购买使用 |

### 2.6 行者协会规则

| 规则 | 说明 |
|---|---|
| 任务来源 | ① 总管理员提供的**善事任务**（救济、资助等无条件任务）② 冒险者提交"任务帮助申请"，经**行者协会管理员审批**后发布 |
| 收益 | 完成善事任务：净化污染（核心收益）+ 少量金币（可配置，建议 D 级区间） |
| 审批流 | 冒险者提交申请 → 管理员审核（通过/驳回+理由）→ 发布为行者任务 |

### 2.7 知识宝库规则

| 规则 | 说明 |
|---|---|
| 投稿 | 冒险者提交**独有经验**（不得侵犯他人隐私，重复内容驳回） |
| 筛选封装 | 知识宝库管理员审核 → 封装为**信息集** → 估价定价 |
| 消费方式 | **购买**（永久阅读权）或 **借阅**（1 天卡 / 周卡 / 月卡，借阅价为购买价的 10%/25%/50%，建议值） |
| 版权约束 | 信息集一经购买，**不可二次售卖/传播牟利**；违规者被**踢出知识宝库权限**（禁止再次投稿、购买、借阅） |
| 运营资金 | 每月总管理员向上月收取营利的 40% 拨给知识宝库供其运行（见 2.8） |

### 2.8 三方资金流（每月结算）

```
冒险者协会（上月佣金 100%）
 ├─ 40% 留存协会
 └─ 60% → 总管理员
            └─ 上月收取营利的 40% → 知识宝库（运营经费）

总管理员 → 每月生产 100 支净化剂 → 冒险者协会（供货）
知识宝库收入 = 购买 + 借阅收入（贡献者分成建议 70%，宝库留成 30%）
```

> ⚠️ 待确认：「每月总管理员需要向知识宝库提供上月收取营利的40%」——本方案按"总管理员上月**收到**的佣金总额的 40%"解释；若原意是"知识宝库自身营利以外由总管理员补 40%"，仅改动结算 job 中金额来源，表结构不变。

### 2.9 角色权限体系（RBAC）

| 角色 | 权限 |
|---|---|
| ADVENTURER 冒险者 | 接单/发单、申请行者任务、商城交易、投稿/购买/借阅、查看自己档案 |
| XINGZHE_ADMIN 行者协会管理员 | 审批任务帮助申请、管理行者任务上下架 |
| ADVENTURER_ADMIN 冒险者协会管理员 | 任务合规审核、商城管理、净化剂上架、封禁执行 |
| KNOWLEDGE_ADMIN 知识宝库管理员 | 知识筛查、信息集封装定价、借阅卡管理、违规处置 |
| SUPER_ADMIN 总管理员 | 提供善事任务、生产净化剂、月度结算、全局配置、账号管理 |

---

## 3. 系统架构

```
┌─────────────────────────────────────────────────┐
│  前端 Vue3 + Element Plus（玩家端 / 管理后台）      │
└──────────────────┬──────────────────────────────┘
                   │ HTTPS / JWT
┌──────────────────▼──────────────────────────────┐
│  SpringBoot 3.x 单体（模块化分包，可平滑拆微服务）   │
│  ├─ auth        认证与会话（JWT + Redis）          │
│  ├─ adventurer  冒险者档案/萌芽/等级               │
│  ├─ task        任务中心（行者/冒险者两源）          │
│  ├─ shop        商城与净化剂                       │
│  ├─ knowledge   知识宝库                          │
│  ├─ finance     金币流水/佣金结算                  │
│  └─ admin       审批/结算/配置                     │
└───┬──────────┬──────────┬──────────┬────────────┘
    │          │          │          │
 MySQL8     Redis      MinIO     RabbitMQ(可选)
 业务数据   会话/锁/排行  图片资源   异步结算/通知
```

**中间件职责**：
- **Redis**：JWT 会话黑名单与在线状态；任务接取分布式锁（防超抢）；污染值/等级缓存；月度排行榜（ZSet）。
- **MinIO**：背景图、称号图片、卡牌、信息集附件（借阅内容走临时签名 URL，到期失效，天然支持借阅时限）。
- **RabbitMQ（可选）**：任务完成异步结算、升级奖励发放、月度结算 Job、站内通知。

---

## 4. 数据库设计（MySQL 8）

### 4.1 ER 总览（核心关系）

```
user 1─1 adventurer          adventurer 1─N task_accept_record N─1 task
user 1─N gold_flow           task N─1 publisher(user)
adventurer 1─N purification_record
xingzhe_apply N─1 applicant(user)
shop_item 1─N trade_order    purification_batch 1─N shop_item
knowledge_contribution 1─1 info_set（审核通过后封装）
info_set 1─N borrow_record / purchase_record
violation_record N─1 user
settlement_log（月度结算流水）
```

### 4.2 表清单与关键字段

**① user（账号表）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 雪花ID |
| username / password | VARCHAR | 登录凭据（BCrypt） |
| nickname | VARCHAR(32) | 昵称 |
| avatar_url | VARCHAR(255) | MinIO 头像 |
| role | VARCHAR(20) | ADVENTURER / XINGZHE_ADMIN / ADVENTURER_ADMIN / KNOWLEDGE_ADMIN / SUPER_ADMIN |
| status | TINYINT | 0 正常 1 禁用 |
| created_at / updated_at | DATETIME | |

**② adventurer（冒险者档案表）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id / user_id | BIGINT | 1:1 关联 |
| rank_level | CHAR(1) | D/C/B/A/S |
| completed_count | INT | 累计完成任务数（每 100 触发升级） |
| pollution_value | TINYINT | 0~100 |
| sprout_status | VARCHAR(10) | NORMAL/GRAY/RED（由污染值推导，冗余存储加速查询） |
| gold_balance | BIGINT | 金币余额（分单位存储避免浮点） |
| banned_until | DATETIME | 协会封禁截止时间，NULL=正常 |
| knowledge_banned | TINYINT | 知识宝库封禁（违规踢出） |

**③ task（任务表，行者/冒险者共用）**
| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | |
| source_type | TINYINT | 1 行者协会 2 冒险者协会 |
| publisher_id | BIGINT | 发布者（行者任务为总管理员/协会） |
| title / description | VARCHAR/TEXT | |
| task_level | CHAR(1) | D/C/B/A/S |
| reward_gold | BIGINT | 报酬（发布者托管金额） |
| acceptor_gold | BIGINT | 接取者实际所得（=90%） |
| purify_value | TINYINT | 净化值（行者善事任务>0） |
| status | TINYINT | 0 待审核 1 发布中 2 进行中 3 待验收 4 完成 5 失败 6 驳回 7 下架 |
| acceptor_id | BIGINT | 接取者 |
| accept_deadline / finish_deadline | DATETIME | |
| audit_id / audit_remark | | 审核信息 |

**④ task_accept_record（接取记录，升降级依据）**
| 字段 | 说明 |
|---|---|
| id, task_id, adventurer_id | 关联 |
| is_cross_level | TINYINT 是否越级接单（升级判定关键） |
| status | 进行中/成功/失败 |
| gold_paid, purify_gained | 结算快照 |
| started_at, finished_at | |

**⑤ purification_record（净化记录）**：adventurer_id、source(善事任务/净化剂/知识收录)、value_before/after、ref_id。

**⑥ xingzhe_apply（行者任务帮助申请）**：applicant_id、title、content、audit_status(待审/通过/驳回)、auditor_id、audit_remark、published_task_id。

**⑦ shop_item（商城商品）**：name、item_type(1 普通 2 净化剂)、price、stock、seller_id（净化剂为冒险者协会管理员）、status、image_url(MinIO)。

**⑧ purification_batch（净化剂批次）**：batch_no、produce_month（每月100支限额校验）、unit_price(50000)、produced_by、remaining。

**⑨ trade_order（交易订单）**：item_id、buyer_id、seller_id、price、type(购买/净化剂)、status、created_at。

**⑩ knowledge_contribution（知识投稿）**：contributor_id、title、content、privacy_declared、audit_status、auditor_id、audit_remark、info_set_id（封装后回填）。

**⑪ info_set（信息集）**：contribution_id、contributor_id、title、summary、content_url(MinIO)、price、status(在售/下架)、sold_count。

**⑫ borrow_record（借阅记录）**：info_set_id、user_id、card_type(1日卡 2周卡 3月卡)、price、start_at、expire_at（借阅内容用 MinIO 临时签名 URL，TTL 对应卡类型）。

**⑬ purchase_record（购买记录）**：info_set_id、buyer_id、price、purchased_at。

**⑭ violation_record（违规记录）**：user_id、type(1 二次售卖)、evidence_url、penalty(1 踢出知识宝库)、handler_id。

**⑮ gold_flow（金币流水）**：user_id、biz_type(任务报酬/佣金/发布托管/升级奖励/商城购买/净化剂/借阅/购买/上缴/拨款)、amount(±)、balance_after、ref_type/ref_id、created_at。

**⑯ association_ledger（协会账本）**：association(1行者 2冒险者 3知识宝库 4总管理员)、month、income、expense、balance。

**⑰ settlement_log（月度结算流水）**：month、type(1 佣金上缴60% 2 知识宝库拨款40% 3 净化剂生产)、amount、from_account、to_account。

**索引建议**：task(status, source_type, task_level)、task_accept_record(adventurer_id, status)、gold_flow(user_id, created_at)、adventurer(rank_level)；金币扣减一律 `UPDATE ... SET balance = balance - ? WHERE balance >= ?` 乐观扣款防并发超支。

---

## 5. API 设计（REST，/api/v1）

### 5.1 认证与档案
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /auth/register /auth/login /auth/logout | JWT 登录，Redis 存会话 |
| GET | /adventurer/me | 我的档案（等级/污染/萌芽状态/余额） |
| GET | /adventurer/rank | 排行榜（Redis ZSet） |

### 5.2 任务中心
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /tasks?source=&level=&status= | 任务列表（行者/冒险者共用） |
| POST | /xingzhe/apply | 冒险者提交行者任务帮助申请 |
| GET | /xingzhe/applies | 管理员审批列表 |
| PUT | /xingzhe/applies/{id}/audit | 审批（通过→自动生成行者任务） |
| POST | /adventurer/tasks | 发布任务（预付托管+10%佣金计算） |
| POST | /tasks/{id}/accept | 接单（Redis 分布式锁防超抢，越级需确认参数） |
| POST | /tasks/{id}/submit /tasks/{id}/verify | 提交/验收（成功→结算+净化+升级判定；失败→封禁30天） |
| POST | /admin/tasks | 总管理员发布善事任务 |

### 5.3 商城
| 方法 | 路径 | 说明 |
|---|---|---|
| GET/POST | /shop/items | 商品列表 / 管理员上架 |
| POST | /admin/purifier/batch | 总管理员生产净化剂（校验月限100支） |
| POST | /shop/orders | 下单购买（扣款→发货→流水） |
| POST | /shop/purifier/use | 使用净化剂（-50污染） |

### 5.4 知识宝库
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /knowledge/contributions | 投稿独有经验 |
| PUT | /knowledge/contributions/{id}/package | 管理员封装成信息集+定价 |
| GET | /knowledge/info-sets | 信息集列表 |
| POST | /knowledge/info-sets/{id}/purchase | 购买 |
| POST | /knowledge/info-sets/{id}/borrow | 借阅（1天/周/月卡） |
| GET | /knowledge/info-sets/{id}/content | 阅读内容（校验购买/借阅有效期，MinIO 临时URL） |
| POST | /knowledge/violations | 违规举报/处置（踢出知识宝库） |

### 5.5 财务与管理
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /finance/flows | 我的金币流水 |
| POST | /admin/settlement/monthly | 触发月度结算（60%上缴 / 40%拨款） |
| GET | /admin/ledger | 协会账本 |
| PUT | /admin/users/{id}/ban | 封禁管理 |

统一响应体 `{code, message, data}`；Knife4j 按模块 `@Tag` 分组；越级接单、购买信息集等高危操作要求二次确认参数 `confirm=true`。

---

## 6. 关键流程时序

**越级任务判定（核心）**
```
接单(校验: 等级≤task_level+2 且 banned_until为空, RED禁S级)
  → 成功: 发放90%报酬 + 净化 + completed_count+1
      ├─ is_cross_level && task_level > 当前等级 → 等级直升为 task_level
      ├─ completed_count % 100 == 0 → 自然升级(D→C→B→A→S) + 发放10,000奖励
      └─ (两者同时满足时取更高等级，只发一次奖励)
  → 失败: banned_until = now+30天，托管报酬按10%佣金规则退还发布者90%
```

**月度结算 Job（每月1日 00:30）**
```
1. 汇总冒险者协会上月佣金 → 60% 转总管理员（settlement_log + 双方 gold_flow）
2. 汇总总管理员上月收到营利 → 40% 拨给知识宝库
3. 校验/重置净化剂当月生产限额（purification_batch）
4. 结算知识宝库贡献者分成（建议70%）
```

---

## 7. 开发里程碑建议

| 阶段 | 内容 |
|---|---|
| P1 | 工程搭建、JWT 认证、用户/冒险者档案、污染与萌芽状态机 |
| P2 | 任务中心（发布/接单/验收/升降级/封禁）+ 金币流水 |
| P3 | 商城 + 净化剂批次 + 行者协会审批流 |
| P4 | 知识宝库（投稿/封装/借阅/购买/违规处置） |
| P5 | 月度结算 Job、排行榜、MinIO 资源、前端联调、剧情文案与 UI 主题 |

---

## 8. 待确认事项（开发前需拍板）

1. **40% 拨款口径**：按"总管理员上月收到佣金的 40%"实现（见 2.8 注）。
2. **越级限制**：是否限制"最多跨 2 级越级"（本方案建议开启，防刷）。
3. **失败惩罚范围**：暂停 1 个月是否同时禁止行者协会与商城？（本方案：仅冒险者协会任务相关功能）。
4. **借阅卡定价**：1天/周/月 = 10%/25%/50% 购买价（建议值，可配置化）。
5. **知识贡献者分成**：建议 70%（可配置）。
6. **善事任务金币收益**：建议 D 级区间并全量走行者协会经费（无佣金）。
