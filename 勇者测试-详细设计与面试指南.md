# 勇者测试 —— 项目详细设计 + Java 面试指南

> 仓库：https://github.com/mqq0000/brave-test
> 技术栈：Java 17 · SpringBoot 3.3 · MyBatis-Plus 3.5 · MySQL 8 · Redis · JWT(jjwt 0.12) · MinIO · Vue 3 · Docker
> 本文档所有设计点均对应仓库真实代码，文件路径可直接定位。

---

# 第一部分：项目详细设计

## 1. 项目概览

**立意**：高污染世界下，每个人的"萌芽"会被环境污染侵蚀——灰化（抑郁倾向）/ 赤化（愤怒倾向）。玩家扮演冒险者，通过完成善事任务、剧情抉择、觉醒测试来净化萌芽、辨认负能量。

**玩法闭环**：

```
注册(觉醒) ──► 接任务 ──► 走剧情/完成善事 ──► 验收结算(金币+净化值)
    ▲                                                    │
    └────── 升级(SABCD五级) ◄── 完成计数 ◄────────────────┘
             商店买净化剂(主动净化) / 知识宝库借阅学习
```

**三条资金规则**（对应三协会）：
1. 任务佣金：协会抽 10%，接单者得 90%
2. 月度结算：冒险者协会佣金 60% 上缴总管理员；总管理员营利 40% 拨付知识宝库
3. 净化剂：5 万金币/支，总管理员月限供应 100 支

## 2. 总体架构

```
浏览器 ──► Nginx(容器:80, 静态资源+反代/api) ──► SpringBoot(容器:8080)
                                                   ├── MySQL 8   (23 张表, utf8mb4)
                                                   ├── Redis     (JWT黑名单/分布式锁/ZSet排行榜/缓存)
                                                   └── MinIO     (知识信息集对象存储, 临时签名URL)

CI: GitHub Actions —— push 触发「后端 mvn test+package」+「前端 vite build」双 Job
```

- **前端**：Vue3 + Vite + Element Plus + Pinia + Router + Axios，8 个页面（Login/TaskHall/Shop/Knowledge/Story/Test/Profile/Admin）
- **后端分层**：Controller → Service → Mapper(MyBatis-Plus)，公共层 `R` 统一响应体、`BusinessException` + `GlobalExceptionHandler` 全局异常、`UserContext`(ThreadLocal) 传递当前用户
- **部署**：docker-compose 五服务编排，MySQL initdb 自动导入 `schema.sql`（建库+23表+种子数据）

## 3. 数据库设计（23 张表，六大域）

| 域 | 表 | 说明 |
|---|---|---|
| 用户域 | `user` | 账号、BCrypt 密文、角色(USER/ADMIN/SUPER)、username 唯一索引 |
| 冒险者域 | `adventurer` | 段位(SABCD)、金币余额、完成计数、萌芽状态(GRAY/RED/PURE)、封禁截止时间 |
| 任务域 | `task`、`task_accept_record`、`xingzhe_apply` | 任务主表(8 状态机)、接单记录(含越级标记)、行者协会审批 |
| 净化域 | `purification_record`、`purification_batch`、`shop_item`、`trade_order` | 净化流水、月度批次(限购 100 支)、商品、订单 |
| 知识域 | `knowledge_contribution`、`info_set`、`borrow_record`、`purchase_record`、`violation_record` | 投稿→封装定价→借阅/购买、违规踢出记录 |
| 财务域 | `gold_flow`、`association_ledger`、`settlement_log` | 个人金流水(balance_after 链式对账)、协会月度账本、结算日志(幂等键) |
| 内容域 | `story_chapter`、`story_node`、`story_option`、`player_story_progress`、`test_question`、`test_result` | 剧情三层结构+玩家进度、题库+测试结果 |

**关键设计决策**：
- **状态机落库**：`task.status` 0待审核→1发布中→2进行中→3待验收→4完成/5失败/6驳回/7下线，所有状态迁移在 Service 层用「条件更新」保证原子性
- **金额用 long（分/金币整数）**，杜绝浮点误差
- **审计字段**：流水表记 `ref_type + ref_id`（业务引用）、`balance_after`（余额快照）——天然支持对账
- **幂等键**：`settlement_log` 的 `month + type` 唯一，防结算 Job 重复执行

## 4. 核心模块设计

### 4.1 认证与鉴权（`security/`）

- 登录签发 JWT（subject=userId，携带 username/role，HS256 + 256bit 密钥）
- `JwtInterceptor`（HandlerInterceptor）：`preHandle` 解析 `Authorization: Bearer xxx` → 校验签名 → 查 Redis 黑名单（登出后 token 立即失效）→ 写入 `UserContext`（ThreadLocal）；`afterCompletion` 强制清理防内存泄漏
- 登出 = token 写入 Redis 黑名单，TTL = token 剩余有效期（无状态 JWT + 有状态黑名单的折中方案）

### 4.2 任务中心（`TaskService`，440 行，项目最复杂模块）

**接单防超抢（双保险）**：
```java
// 第一层：Redis SETNX 分布式锁（10s 过期）
Boolean locked = redisTemplate.opsForValue().setIfAbsent(lockKey, "1", Duration.ofSeconds(10));
// 第二层：数据库条件更新（乐观占单）——只认「仍处于发布中」的任务
int rows = taskMapper.update(null, new LambdaUpdateWrapper<Task>()
        .eq(Task::getId, taskId).eq(Task::getStatus, ST_OPEN)   // WHERE status=1
        .set(Task::getStatus, ST_IN_PROGRESS).set(Task::getAcceptorId, userId));
if (rows == 0) throw new BusinessException("手慢了，任务已被其他冒险者接取");
```
> 代码注释里明确承认：锁在事务提交前释放存在极小竞态窗口，**条件更新兜底保证正确性**——这是"锁优化体验、DB 约束保正确"的典型分层思维。

**升降级规则**：
- 越级成功 → 直升至任务等级；越级失败 → 封禁 30 天（`banned_until`）；每完成 100 件自然升级 +1 万奖励；越级与自然升级同时满足时取更高等级、奖励只发一次
- 越级需显式传 `confirmCrossLevel=true`（服务端拒绝到返回 400 提示，前端二次确认弹窗）

**资金托管**：发布即全额托管（`ESCROW` 扣款）→ 验收成功发 90% + 佣金入协会账本 → 失败退发布者 90%（佣金不退）→ 审核驳回退全款

### 4.3 财务系统（`FinanceService`）

- **乐观扣款防超支**：`UPDATE adventurer SET gold_balance = gold_balance - ? WHERE id = ? AND gold_balance >= ?`，影响行数=0 即余额不足，不用悲观锁、不查后改
- **金流水**：每笔进出记 `gold_flow`（金额 + 变动后余额快照 + 业务引用），余额可全程追溯
- **协会账本**：`association_ledger` 按协会+月份记 income/expense/balance 三列增量

### 4.4 月度结算（`SettlementService`）

- `@Scheduled(cron = "0 30 0 1 * ?")` 每月 1 日 00:30 自动跑，也支持管理员手动触发补算历史月份
- **幂等设计**：每笔结算写 `settlement_log`（month+type 唯一），执行前查重——Job 重复跑、服务重启重跑都不会重复转账
- 链式结算：冒险者协会佣金的 60% 上缴 → 总管理员收到的营利的 40% 拨给知识宝库

### 4.5 知识宝库（`KnowledgeService`，332 行）

- 流程：投稿 → 知识协会定价封装（正文存 MinIO，key 形如 `info/{id}/content.txt`）→ 借阅(日/周/月卡)或买断
- **防盗链**：正文不落库直出，而是生成 **MinIO 临时签名 GET URL**，过期时间与借阅有效期联动——借阅到期 URL 自动失效
- **Host 重写**：容器内签名 URL 生成的是 `http://minio:9000/...`，浏览器不可达。`MinioService.presignedGetUrl` 解析 URI，把 host 部分替换为配置的 `minio.public-endpoint`（如 `http://localhost:9000`）
- 降级：MinIO 不可用时上传失败仅告警并回退数据库存储；二次售卖违规 → `violation_record` + 封号

### 4.6 剧情引擎 & 觉醒测试

- **三层结构**：chapter(章节，解锁链) → node(剧情节点) → option(选项，绑定污染度/金币/下一节点)。玩家进度表 `player_story_progress` 记当前节点 + 完成章节，选择即实时结算
- **觉醒测试**：题目按 勇气/理性/仁善 三维计分 → 加权映射称号 → 结果落 `test_result`

### 4.7 排行榜

- Redis **ZSet**（`ZINCRBY` 累计完成任务数），`ZREVRANGE` 取 TopN——O(logN) 写入、免全表排序

## 5. 工程化设计

| 项 | 实现 |
|---|---|
| 统一响应 | `R<T>`（code/message/data），`GlobalExceptionHandler` 捕获 BusinessException/参数校验/未知异常 |
| 接口文档 | Knife4j 4.5 + springdoc，注解扫 Controller 自动生成 `/doc.html` |
| 配置外置 | `application.yml` 只放开发默认值；compose 用 `SPRING_DATASOURCE_*` 等**环境变量覆盖**（Spring 松绑定：`MINIO_ACCESS_KEY` 下划线而非连字符） |
| 容器化 | 后端多阶段 Dockerfile（Maven 构建 → JRE 运行）；前端构建产物由 Nginx 托管；MySQL initdb 目录自动导 schema |
| CI | GitHub Actions 双 Job：temurin 17 + Maven 缓存跑单测打包；Node 20 跑前端 build |
| 测试 | 单测 13 用例（JUnit5 + MP 内存初始化）；API 回归 38 用例（Python requests）；浏览器 E2E 12 用例（playwright-core） |

---

# 第二部分：Java 面试可能问到的问题（含参考答案 + 项目落点）

## A. Spring / SpringBoot

**Q1：`@Transactional` 在哪些情况下会失效？你项目里踩过吗？**
失效场景：① 同类自调用（绕过 AOP 代理）；② 方法非 public；③ 异常被 catch 吞掉；④ 默认只回滚 RuntimeException/Error，受检异常需 `rollbackFor`；⑤ 传播行为配错；⑥ 多线程调用。
**项目落点**：StoryService 早期在一个方法里 `this.xxx()` 自调用带事务的方法导致事务失效，后重构为拆分到不同 Service 依赖注入调用。这是最能加分的实战回答。

**Q2：`@Transactional` 加在类上和方法上有什么区别？事务的传播行为（PROPAGATION）说几个？**
类级对所有 public 方法生效。常用传播：REQUIRED（默认，有则加入无则新建）、REQUIRES_NEW（挂起当前开新事务，适合"无论主事务成败都要记日志"）、NESTED（保存点）、SUPPORTS/NOT_SUPPORTED。项目里扣款、流水、状态更新都在同一 REQUIRED 事务里，保证"扣钱必记流水"。

**Q3：拦截器（HandlerInterceptor）和过滤器（Filter）的区别？为什么 JWT 校验用拦截器？**
Filter 是 Servlet 规范、在 DispatcherServlet 之前、拿不到 Handler 信息；Interceptor 是 Spring MVC 机制、可感知目标 Handler、能注入 Spring Bean、有 preHandle/afterCompletion 生命周期。JWT 校验需要读取路由配置、抛业务异常交给全局处理器，且要与 UserContext 生命周期绑定，拦截器更合适。

**Q4：ThreadLocal 有什么风险？你的 UserContext 怎么处理的？**
线程池场景下线程复用导致脏数据（用户 A 看到用户 B 的数据）和内存泄漏。必须 `afterCompletion` 里 `UserContext.clear()`。追问"为什么 tomcat 线程会复用"→ 线程池核心思想。

**Q5：SpringBoot 自动装配原理？**
`@EnableAutoConfiguration` → `AutoConfigurationImportSelector` 读取 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`（3.x 新位置），按 `@ConditionalOnClass/@ConditionalOnMissingBean` 条件装配。项目里自己写的 RedisTemplate 序列化配置 Bean 会覆盖默认模板（`@ConditionalOnMissingBean` 让位）。

**Q6：全局异常处理怎么做的？为什么用 RuntimeException 子类？**
`@RestControllerAdvice + @ExceptionHandler`。`BusinessException extends RuntimeException`——RuntimeException 默认触发回滚，业务异常不用到处 try-catch 也不用声明 throws，配合事务最省心。

## B. MyBatis / MyBatis-Plus

**Q7：MyBatis 中 `#{}` 和 `${}` 的区别？**
`#{}` 预编译占位符（防 SQL 注入），`${}` 字符串拼接（有注入风险）。项目里只有 `setSql("gold_balance = gold_balance - " + amount)` 处因 amount 是服务端校验过的 long 才允许拼接——如果被问代码审计点，这里是现成素材，可顺势说"更严谨应改用参数绑定"。

**Q8：MyBatis-Plus 的 LambdaQueryWrapper 相比字符串 Wrapper 好在哪？**
`Task::getStatus` 方法引用编译期检查字段名，重构字段不会产生运行期字符串错误。`eq(condition, column, value)` 三参重载做动态条件，免 if 拼接。

**Q9：不写 SQL 怎么实现"字段原子增减 + 条件更新"？**
`LambdaUpdateWrapper` + `setSql` + 条件 eq/ge（见 Q:乐观锁），本质是把业务校验下沉到 UPDATE 的 WHERE 子句，用**受影响行数**作为并发仲裁结果。

**Q10：MyBatis 一级/二级缓存？**
一级缓存 SqlSession 级默认开启（Spring 集成下每次请求新 session，基本无感）；二级缓存 namespace 级，跨 session，分布式下易脏读一般不开。项目未用二级缓存，金币余额等强一致数据直接走 DB 条件更新。

## C. MySQL

**Q11：扣款为什么写 `SET balance = balance - ? WHERE balance >= ?` 而不是"先查再改"？**
先查再改（check-then-act）在并发下超卖；条件更新由 MySQL 行锁保证原子性，WHERE `gold_balance >= amount` 把余额校验和扣减合并为一步，`affected rows = 0` 即失败。这是**乐观锁思想**（也可用 version 号实现标准 CAS）。

**Q12：接单场景用了 Redis 锁，为什么还要数据库条件更新？锁不是够了吗？**
① 锁有过期时间，业务超时会提前失效；② 锁在事务提交前 finally 释放，其他请求可能拿到锁后读到未提交的旧状态；③ Redis 主从切换可能丢锁。**锁只做流量削峰/体验优化，数据正确性交给 DB 状态机**。能主动讲出"锁释放早于事务提交"这个竞态窗口是高频考点。

**Q13：InnoDB 事务隔离级别？项目默认用了什么，有什么并发现象？**
默认 REPEATABLE READ（MVCC + 间隙锁防幻读）。项目读多写少，扣款/占单靠 UPDATE 行锁串行化，普通 SELECT 走快照读不阻塞。

**Q14：表为什么用 utf8mb4 而不是 utf8？**
MySQL 的 utf8 是残缺的 3 字节实现，存不下 emoji 和部分生僻字；utf8mb4 才是真 UTF-8。项目实际踩过：命令行客户端 latin1 连接导入 schema 出现乱码，最终在 schema.sql 开头加 `SET NAMES utf8mb4;` 治本。

**Q15：幂等怎么设计的？**
① 结算 Job：`settlement_log` 的 `month+type` 业务唯一键，先查后写 + DB 约束兜底；② 金币流水带 `ref_type/ref_id` 可追溯对账。追问可延伸：接口幂等的通用手段（唯一索引、token 机制、状态机前置校验）。

**Q16：task 表的 status 状态机为什么不用枚举类型/字符串？**
DB 存 tinyint + Java 常量类（`ST_OPEN` 等），省空间、可加索引、状态迁移由条件更新守门（`WHERE status=旧值`），非法迁移在 SQL 层直接失败。

## D. Redis

**Q17：分布式锁为什么用 `setIfAbsent(key, value, ttl)` 一条命令？**
SETNX + EXPIRE 分两条命令在中间宕机会造成死锁；Redis 2.6.12+ 的 `SET key value NX EX` 原子完成。Spring 里即 `opsForValue().setIfAbsent(key, "1", Duration.ofSeconds(10))`。追问：如何防误删他人锁（value 存唯一标识 + Lua 校验删除）、看门狗续期（Redisson）。

**Q18：JWT 无状态，为什么要再查 Redis 黑名单？不自相矛盾吗？**
JWT 的无状态换来了"无法主动作废"。登出/封号场景需要立即失效能力，所以折中：正常请求只验签名不查库的设想成立，登出时把 token 写黑名单（TTL=剩余有效期）。这是典型的**一致性 vs 性能权衡**，能讲清 trade-off 比背结论重要。

**Q19：排行榜为什么用 ZSet？**
底层跳表 + 哈希表，`ZINCRBY` O(logN) 累加、`ZREVRANGE WITHSCORES` 直接取 TopN，避免 `ORDER BY completed_count LIMIT 10` 的全表/索引扫描，且天然去重按 userId。

**Q20：Redis 为什么你的项目里没做 MySQL 缓存？什么数据适合上缓存？**
金币余额、任务状态是强一致写密集数据，缓存反而引入一致性问题；适合缓存的是读多写少数据（如任务大厅列表、剧情文本）。能说出"不是所有数据都该加缓存"是加分项。

## E. JWT / 安全

**Q21：JWT 的结构？怎么防篡改？防伪造？**
Header.Payload.Signature，HS256 对前两段做 HMAC 签名。篡改 payload 会导致签名不匹配；防伪造靠密钥保密（项目密钥从环境变量注入而非硬编码）。注意 JWT **不加密**，payload base64 可解——不能放敏感信息。

**Q22：密码怎么存的？**
BCrypt（自带随机盐 + 慢哈希），项目种子管理员密码就是用 BCrypt 现生成的密文。追问 BCrypt vs MD5+盐：BCrypt 有 cost 因子可随算力提升调慢，抗彩虹表/暴力破解更强。

**Q23：token 放哪？怎么防 XSS/CSRF？**
前端存 Pinia + localStorage 并由 Axios 拦截器统一注入 `Authorization: Bearer`。JWT 走 Header 不走 Cookie 天然免疫 CSRF；XSS 靠 Vue 模板默认转义。

## F. 并发 / JVM / 设计

**Q24：项目里有哪些并发控制手段？对比一下。**
① 数据库条件更新（乐观锁，正确性兜底）；② Redis SETNX 分布式锁（单机 Redis，削峰+防无效请求打到 DB）；③ 唯一约束/幂等键（结算）；④ ThreadLocal 隔离用户上下文。对比维度：性能、正确性保证、适用规模（再大规模可提 Redisson/RedLock、DB SELECT FOR UPDATE 悲观锁方案及各自代价）。

**Q25：如果要加"秒杀净化剂"场景怎么设计？**
现状 `purification_batch` 月限 100 支已有条件扣减模型，扩展：Redis 预扣库存（DECR + 判负回补）→ MQ 削峰 → DB 最终扣减 + 唯一订单键幂等。这题能把项目经验迁移到经典场景。

**Q26：单测里 `TableInfoHelper.initTableInfo` 是干嘛的？为什么需要？**
纯单测（不起 Spring 容器）中 MyBatis-Plus 的 LambdaWrapper 依赖实体的 TableInfo 缓存（lambda cache），直接 new Service 调用会报 `can not find lambda cache`。`@BeforeAll` 里用 `new MapperBuilderAssistant(new MybatisConfiguration(), "")` 手动初始化实体元数据。这是 MP 单测的高频坑。

**Q27：Dockerfile 多阶段构建解决什么问题？**
构建阶段（Maven + 全量依赖）和运行阶段（仅 JRE + jar）分离，镜像不含构建工具链，体积小、攻击面小。compose 里 `SPRING_DATASOURCE_PASSWORD` 这类变量体现**12-Factor 配置外置**。

**Q28：@Scheduled 定时任务多实例部署会重复执行怎么办？**
当前单实例没问题；扩展方案：ShedLock/Redis 锁抢占、XXL-JOB 分布式调度、或消息队列只投递一次。项目用 `settlement_log` 幂等键已经把"重复执行"的危害消掉了——**幂等是分布式任务的第一道防线**。

---

## 附：自我介绍时的项目一句话模板

> "我独立设计开发了一个全栈游戏项目：SpringBoot 3 + MyBatis-Plus 后端、23 张表支撑三协会经济体系；重点解决了**并发接单防超抢（Redis 锁 + DB 条件更新双保险）、乐观扣款防超支、月度结算幂等、MinIO 签名 URL 防盗链**等问题；配套 JUnit 单测、38 例 API 回归、Playwright E2E 和 GitHub Actions CI，docker-compose 一键部署。"

---

# 第三部分：核心源码详解（真实代码 + 逐段讲解）

> 以下代码全部摘自仓库 `brave-test-backend`，标注了文件路径，可对照阅读。

## 3.1 统一响应与全局异常（`common/`）

```java
// common/GlobalExceptionHandler.java —— 全局异常兜底
@Slf4j
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)               // 业务异常 → 原样透出
    public R<Void> handleBusiness(BusinessException e) {
        return R.fail(e.getCode(), e.getMessage());
    }

    @ExceptionHandler(MethodArgumentNotValidException.class) // 参数校验失败 → 400
    public R<Void> handleValid(MethodArgumentNotValidException e) {
        FieldError fe = e.getBindingResult().getFieldError();
        String msg = fe == null ? "参数校验失败" : fe.getDefaultMessage();
        return R.fail(400, msg);
    }

    @ExceptionHandler(Exception.class)                       // 未知异常 → 500 且不泄露堆栈
    public R<Void> handleOther(Exception e) {
        log.error("系统异常", e);
        return R.fail(500, "系统繁忙，请稍后再试");
    }
}
```

**讲解**：三层异常分级——业务异常给用户看、参数异常提示改参、未知异常只记日志对外说"系统繁忙"（防止堆栈信息泄露）。Controller 里因此**零 try-catch**。

```java
// common/UserContext.java —— ThreadLocal 用户上下文
private static final ThreadLocal<UserInfo> HOLDER = new ThreadLocal<>();

public static Long getUserId() {
    UserInfo info = HOLDER.get();
    if (info == null) throw new BusinessException(401, "未登录或会话已过期");
    return info.getUserId();
}

public static void clear() { HOLDER.remove(); }  // 拦截器 afterCompletion 强制调用
```

**讲解**：Controller/Service 里不用传 userId 参数，`UserContext.getUserId()` 即取。风险点在 Tomcat 线程池复用线程，所以拦截器 `afterCompletion` 必须 `clear()`，否则下一个请求可能读到上一个用户的身份（面试必问的 ThreadLocal 内存泄漏/脏数据）。

## 3.2 JWT 签发与鉴权（`security/`）

```java
// security/JwtUtil.java —— jjwt 0.12 新 API
public String create(Long userId, String username, String role) {
    Date now = new Date();
    Date exp = new Date(now.getTime() + expireHours * 3600_000L);
    return Jwts.builder()
            .subject(String.valueOf(userId))     // subject 放 userId
            .claim("username", username)
            .claim("role", role)
            .issuedAt(now).expiration(exp)
            .signWith(key())                     // HS256，密钥来自环境变量注入
            .compact();
}

public Claims parse(String token) {
    return Jwts.parser().verifyWith(key()).build()
            .parseSignedClaims(token).getPayload();  // 验签+过期校验一体完成
}
```

```java
// security/JwtInterceptor.java
@Override
public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
    String auth = request.getHeader("Authorization");
    if (auth == null || !auth.startsWith("Bearer "))
        throw new BusinessException(401, "未登录或缺少令牌");
    String token = auth.substring(7);
    Claims claims;
    try {
        claims = jwtUtil.parse(token);
    } catch (JwtException | IllegalArgumentException e) {
        throw new BusinessException(401, "令牌无效或已过期");
    }
    Boolean black = redisTemplate.hasKey("jwt:blacklist:" + token);  // 登出黑名单
    if (Boolean.TRUE.equals(black))
        throw new BusinessException(401, "令牌已注销，请重新登录");
    UserContext.set(new UserContext.UserInfo(
            Long.valueOf(claims.getSubject()),
            claims.get("username", String.class),
            claims.get("role", String.class)));
    return true;
}

@Override
public void afterCompletion(HttpServletRequest req, HttpServletResponse resp,
                            Object handler, Exception ex) {
    UserContext.clear();   // 防线程池复用导致身份串号
}
```

**讲解**：验签（防篡改）+ 过期校验（jjwt 自动）+ 黑名单（可主动作废）三道关卡，全部通过才放行。密钥 `jwt.secret` 由 application.yml 默认值 + compose 环境变量 `JWT_SECRET` 覆盖，生产不硬编码。

## 3.3 接单防超抢：Redis 锁 + DB 条件更新双保险（`TaskService.acceptTask`）

```java
@Transactional
public Long acceptTask(Long userId, Long taskId, boolean confirmCrossLevel) {
    String lockKey = "lock:task:accept:" + taskId;
    // ① Redis SETNX 原子加锁，10s 自动过期防死锁
    Boolean locked = redisTemplate.opsForValue()
            .setIfAbsent(lockKey, "1", Duration.ofSeconds(10));
    if (!Boolean.TRUE.equals(locked))
        throw new BusinessException("任务正在被其他冒险者接取，请稍后重试");
    try {
        Adventurer adv = financeService.requireAdventurerByUserId(userId);
        checkNotBanned(adv);                                  // 封禁校验
        Task task = taskMapper.selectById(taskId);
        if (task.getStatus() != ST_OPEN)
            throw new BusinessException("任务当前不可接取");
        if ("RED".equals(adv.getSproutStatus()) && "S".equalsIgnoreCase(task.getTaskLevel()))
            throw new BusinessException("萌芽赤化，无法接取 S 级任务，先去净化萌芽吧");

        int ownIdx = levelIndex(adv.getRankLevel());
        int taskIdx = levelIndex(task.getTaskLevel());
        boolean crossLevel = taskIdx > ownIdx;
        if (crossLevel) {
            if (taskIdx - ownIdx > MAX_CROSS_LEVEL_GAP)       // 最多越 2 级
                throw new BusinessException("最多允许越级挑战 2 级的任务");
            if (!confirmCrossLevel)                           // 越级需二次确认
                throw new BusinessException(400, "越级接单风险极大，失败将被协会暂停开放一个月…");
        }

        // ② DB 条件更新兜底：WHERE status=1 才允许占单，影响行数=0 即被抢
        int rows = taskMapper.update(null, new LambdaUpdateWrapper<Task>()
                .eq(Task::getId, taskId)
                .eq(Task::getStatus, ST_OPEN)                 // 只认"仍处于发布中"
                .set(Task::getStatus, ST_IN_PROGRESS)
                .set(Task::getAcceptorId, userId));
        if (rows == 0)
            throw new BusinessException("手慢了，任务已被其他冒险者接取");

        // ③ 接单记录（越级标记落库，验收时据此决定直升/封禁）
        TaskAcceptRecord record = new TaskAcceptRecord();
        record.setTaskId(taskId); record.setAdventurerId(adv.getId());
        record.setIsCrossLevel(crossLevel ? 1 : 0); record.setStatus(0);
        acceptRecordMapper.insert(record);
        return record.getId();
    } finally {
        redisTemplate.delete(lockKey);                        // ④ finally 释放锁
    }
}
```

**讲解（面试高光段）**：
- 锁的作用是**削峰**——让并发请求快速失败而不是全部打到 DB；
- 真正保证正确性的是 ② 的条件更新：即使锁提前过期/释放，两个请求同时通过 ①，也只有一个能把 `status` 从 1 改成 2；
- 代码注释里主动写了"锁在事务提交前释放存在极小竞态窗口"——能自己指出自己方案的边界条件，比"完美方案"更能体现工程判断力；
- 延伸：要更严谨可以锁值存 UUID、Lua 脚本校验删除，或把锁的释放挪到事务提交后（`TransactionSynchronization`）。

## 3.4 乐观扣款防超支（`FinanceService`）

```java
@Transactional
public void deductGold(Long adventurerId, long amount, String bizType, String refType, Long refId) {
    if (amount <= 0) throw new BusinessException("扣款金额必须大于0");
    // UPDATE adventurer SET gold_balance = gold_balance - ?
    // WHERE id = ? AND gold_balance >= ?      ← 余额校验下沉到 WHERE，行锁下原子完成
    int rows = adventurerMapper.update(null, new LambdaUpdateWrapper<Adventurer>()
            .eq(Adventurer::getId, adventurerId)
            .ge(Adventurer::getGoldBalance, amount)
            .setSql("gold_balance = gold_balance - " + amount));
    if (rows == 0) throw new BusinessException("金币不足");

    Adventurer adv = adventurerMapper.selectById(adventurerId);
    saveFlow(adv.getUserId(), -amount, adv.getGoldBalance(), bizType, refType, refId);
    // saveFlow: 每笔流水记 金额+变动后余额快照+业务引用 → 可全程对账
}
```

**讲解**：对比"先 SELECT 查余额再 UPDATE"——check-then-act 在并发下会超卖；这里用**受影响行数**做仲裁，`rows==0` 即余额不足。同一事务里扣款与流水要么都成要么都不成（REQUIRED 传播）。

## 3.5 月度结算幂等（`SettlementService`）

```java
@Scheduled(cron = "0 30 0 1 * ?")                 // 每月1日 00:30
public void scheduledSettle() {
    String lastMonth = LocalDateTime.now().minusMonths(1)
            .format(DateTimeFormatter.ofPattern("yyyy-MM"));
    settle(lastMonth);
}

@Transactional
public void settle(String month) {
    // 幂等键：settlement_log 的 month + type。执行前查重，Job 重跑不重复转账
    if (settlementLogMapper.selectCount(new LambdaQueryWrapper<SettlementLog>()
            .eq(SettlementLog::getMonth, month)
            .eq(SettlementLog::getType, 1)) == 0) {
        long commissionIncome = getIncome(ASSOC_ADVENTURER, month);
        long toSuper = Math.round(commissionIncome * 0.60);   // 佣金 60% 上缴
        if (toSuper > 0) {
            financeService.ledgerDelta(ASSOC_ADVENTURER, month, 0, toSuper); // 支出
            financeService.ledgerDelta(ASSOC_SUPER, month, toSuper, 0);      // 收入
            saveLog(month, 1, toSuper, ASSOC_ADVENTURER, ASSOC_SUPER);       // 幂等凭证
        }
    }
    // 结算2 同构：总管理员营利 40% 拨付知识宝库（type=2）
}
```

**讲解**：定时任务的第一道防线是**幂等**而不是分布式锁——即使多实例重复触发，幂等键也能保证只结算一次。`settle(month)` 支持指定月份，管理员可手动补算历史账。

## 3.6 MinIO 临时签名 URL 与 Host 重写（`MinioService`）

```java
public String presignedGetUrl(String objectKey, int expirySeconds) {
    String url = minioClient.getPresignedObjectUrl(GetPresignedObjectUrlArgs.builder()
            .method(Method.GET).bucket(bucket).object(objectKey)
            .expiry(expirySeconds, TimeUnit.SECONDS)     // 到期自动失效 = 防盗链
            .build());
    // 容器内生成的是 http://minio:9000/...，浏览器不可达 → 替换 host 为外部可达地址
    if (publicEndpoint != null && !publicEndpoint.isBlank()) {
        java.net.URI uri = java.net.URI.create(url);
        String hostPart = uri.getScheme() + "://" + uri.getRawAuthority();
        String publicBase = publicEndpoint.replaceAll("/$", "");
        if (url.startsWith(hostPart) && !hostPart.equals(publicBase)) {
            url = publicBase + url.substring(hostPart.length());
        }
    }
    return url;
}
```

**讲解**：两个考点——①签名 URL 让存储桶保持私有，访问权通过带签名的临时链接授予（AWS S3 同款机制）；②Docker 网络内外主机名不一致是容器化经典坑，服务端签发后重写 host，签名参数只与路径+过期时间有关所以重写安全。

## 3.7 知识宝库：借阅/购买与 TTL 联动（`KnowledgeService`）

```java
// 借阅：1天卡10% / 周卡25% / 月卡50% 的价格，到期失去阅读权
@Transactional
public Long borrow(Long buyerUserId, Long infoSetId, int cardType) {
    Adventurer buyer = requireNotKnowledgeBanned(buyerUserId);  // 违规者永久禁入
    InfoSet infoSet = infoSetMapper.selectById(infoSetId);
    long price = Math.max(1, Math.round(infoSet.getPrice() * CARD_RATE[cardType]));
    financeService.deductGold(buyer.getId(), price, "KNOWLEDGE_BORROW", "INFO_SET", infoSetId);

    BorrowRecord record = new BorrowRecord();
    record.setStartAt(LocalDateTime.now());
    record.setExpireAt(LocalDateTime.now().plusDays(CARD_DAYS[cardType]));  // 日/周/月
    borrowRecordMapper.insert(record);
    settleRevenue(infoSet, price);      // 70% 分给贡献者，30% 入知识宝库账本
    return record.getId();
}

// 阅读：签名 URL 有效期 = min(1小时, 借阅剩余时间) —— 到期自动打不开
public ReadVO read(Long userId, Long infoSetId) {
    // …校验已购买 或 持有未过期借阅，否则 403…
    long ttl = 3600;
    if (bought == 0) {
        long remain = Duration.between(LocalDateTime.now(), borrow.getExpireAt()).getSeconds();
        ttl = Math.min(ttl, remain);        // 借阅者：不超过剩余借阅时间
    }
    if (ttl > 0) {
        return new ReadVO("url", minioService.presignedGetUrl(infoSet.getContentUrl(), (int) ttl));
    }
    return new ReadVO("text", fallback);    // MinIO 不可用时回退数据库文本
}
```

**讲解**：内容不下发明文链接，而是"每次阅读现签发、有效期跟随借阅权限"——权限模型与存储层联动，借阅过期后即使拿到旧链接也打不开。分账（70/30）与扣款同事务，保证账实一致。

## 3.8 剧情引擎：章节解锁链与选择结算（`StoryService`）

```java
// 章节列表：前一章 completed 才解锁下一章（解锁链）
public List<Map<String, Object>> listChapters(Long userId) {
    List<StoryChapter> chapters = chapterMapper.selectList(
            new LambdaQueryWrapper<StoryChapter>().orderByAsc(StoryChapter::getSeq));
    List<Map<String, Object>> result = new ArrayList<>();
    boolean prevCompleted = true;
    for (StoryChapter chapter : chapters) {
        PlayerStoryProgress progress = progressMapper.selectOne(…);
        boolean unlocked = prevCompleted;
        boolean completed = progress != null && progress.getStatus() == 1;
        // …组装 unlocked/completed/inProgress…
        prevCompleted = completed;      // 本章完成状态成为下一章的解锁条件
    }
    return result;
}

// 做出选择：结算污染/金币，推进到下一节点；next 为空则本章完结
@Transactional
public Map<String, Object> choose(Long userId, Long optionId) {
    StoryOption option = optionMapper.selectById(optionId);
    PlayerStoryProgress progress = progressMapper.selectOne(…当前进行中的进度…);
    // 1. 选项绑定 pollutionDelta / goldDelta → 实时结算
    // 2. progress.currentNodeId = option.getNextNodeId()，choicesLog 追加选择轨迹
    // 3. nextNode == null → 本章完结，status 置 1（触发下一章解锁）
    // 4. renderNode 返回新节点文本与选项列表
}
```

**讲解**：章节→节点→选项三层是**图结构**（option 指向 nextNodeId，可分支可汇合），玩家进度表只存"当前指针 + 选择日志"，天然支持断点续玩；所有结算走事务，选择产生的金币变化与金流水一致。

---

## 源码阅读建议路线

1. `common/`（R、BusinessException、GlobalExceptionHandler、UserContext）→ 10 分钟，看懂工程骨架
2. `security/`（JwtUtil、JwtInterceptor）→ 认证链路
3. `TaskService.acceptTask` → 并发控制精华
4. `FinanceService` → 事务与对账设计
5. `SettlementService` → 定时任务幂等
6. `KnowledgeService` → 业务最复杂，权限+存储+分账联动
7. `sql/schema.sql` → 23 张表与外键关系
